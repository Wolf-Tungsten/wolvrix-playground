# Goal: 优化 GrhSIM-IR 单线程 XiangShan CoreMark 100k

## 目标与范围

通过多个完整节点积累可组合的性能改进，逐步逼近 **gsim 性能**，不要求单个方案一步达标。整个 goal 完成须同时满足：100k 行为等价、单线程仿真性能提升经 6 次新旧交替（3 新 + 3 旧）复测的统计分析确认为真实而非噪声（2026-09-12 用户调整，取代原 3% 硬门禁与独立复跑要求）、完整 SV→C++ 生成和 C++ 编译各 `<1800 s`，以及完整归档已提交。

允许修改 GrhSIM-IR 语义、op、emit、分区、调度及等价子图/状态访问优化。禁止修改冻结的 GRH IR、GRH 上已有 pass、XiangShan 和测试源码；禁止多线程仿真及模块名称匹配优化。方案必须由语义、依赖、位宽、读写属性等通用特征触发。

优先探索能大幅减少主要成本的创新机制，如共享重复状态/事件历史、消除重复求值、跨子图融合、专用 op 或执行机制重构。每个假设须说明新意、瓶颈证据、覆盖面、预期收益范围或上界及可证伪标准。不能长期围绕边角 helper 或同一参数微调；局部调整应服务于核心方案。单次尝试允许有证据的失败并须如实记录；但节点不得以失败告终，每个节点必须取得经统计确认的真实性能收益（2026-09-12 用户调整）。不以创新为由放宽正确性和时间门槛。

## 节点与阶段

**每次 `/goal` 完成一个节点：围绕一个核心优化方向从假设提出推进到实验验证，直到取得经统计确认的真实性能收益后统一提交并停止；不允许以 `REJECTED` 作为节点最终判定（2026-09-12 用户调整）。** 阶段是节点内部状态，不是独立的 `/goal` 或提交点：

| 阶段 | 必要工作 |
|---|---|
| `IDEA` | 提出假设、创新机制、局部收益目标和验证标准 |
| `BASELINE` | 核对输入，建立或复用对照，确定资源配置、计时边界和止损线 |
| `IMPLEMENTED` | 实现方案，记录语义约束和版本差异，通过聚焦测试 |
| `VALIDATED` | 完成全流程门槛、100k 等价检查、6 次新旧交替复测与统计分析 |
| `ACCEPTED` | 局部收益经 6 次交替复测统计分析确认为真实，正确性和生成/编译门槛通过 |
| `REJECTED` | 节点内单次尝试被否定的过程记录（假设证伪、收益不足或验证失败及理由），不能作为节点最终判定 |
| `FAILED` | 节点级终止判定：**只能由用户人工判定并登记（2026-09-20 用户调整）。AI 不得主动放弃、不得以任何理由自行将节点标记为 `FAILED` 或变相终止推进（含"诊断节点"等名义）；证据不足、候选证伪、收益低于噪声等均不构成 AI 停止的理由，AI 只能继续修正/改换机制直至 `ACCEPTED`，或等待用户判定 |

- 各阶段在同一报告中及时记录计划、方法、结果和决策，留在工作区供观察；不预填结果，不作阶段提交。
- 基线测量、工具准备、实现和测试均在当前节点内完成，不能拆成多次 `/goal` 代替方案验证。
- `ACCEPTED` 只表示节点局部接受，不要求已达到 gsim 性能；收益幅度不设固定下限，但必须通过 6 次交替复测的统计分析排除噪声，噪声范围内的差异不能当作已证明收益。
- 证据足以否定假设时，将该次尝试记为 `REJECTED` 并分析原因，随后在同一节点内修正机制或改换机制继续实验，直至取得真实收益；节点的唯一完成判据是 `ACCEPTED`。`FAILED` 仅由用户人工判定（2026-09-20 用户调整）：AI 在任何情况下不得自行宣告节点失败或停止推进，只能继续实验或等待用户指示。
- 自动续行、工具调用和上下文切换只延续当前节点。节点完成后，须由用户再次启动 `/goal` 才能开始下一节点。
- 用户中断或外部阻塞时保留工作区和阶段记录，恢复后继续同一节点；不得把中断当作节点完成或提前提交。

## 执行与验证

所有构建、安装、生成、测试和仿真均通过 Makefile 目标；缺少目标时先补充，禁止手拼底层命令。常用目标为 `xs_wolf_grhsim_ir`、`xs_wolf_grhsim_ir_build_emu`、`run_xs_wolf_grhsim_ir_emu`，以当前 Makefile 为准。

每个节点有唯一 ID，每次实验有唯一 `RUN_ID`。日志和临时产物放在 `ptmp/`，不写 `/tmp`。仿真固定 CPU 并显式设置 `XS_EMU_THREADS=1`；编译按机器实际核数并行——`xs_wolf_grhsim_ir_build_emu` 默认 `VM_BUILD_JOBS=$(XS_VM_BUILD_JOBS)`（`nproc`，2026-09-18 订正，此前固定默认 4 曾致 NO00041/42 编译计时虚高约 3.4 倍），不得沿用固定小并行度；节点文档须记录实际 job 数。

| 区间 | 计时边界与门槛 |
|---|---|
| SV→C++ 生成 | Make 启动至完整目标 C++ 和 Makefile 生成完成，墙钟 `<1800 s`；恢复已有 IR 的时间不能替代完整 SV 路线 |
| C++ 编译 | 编译目标启动至 emu 可执行文件完成，墙钟 `<1800 s` |
| 仿真 | 仅 emu 执行区间；记录 Host/墙钟时间、cycle 上限、退出状态及等价性 |

启动前安装截止机制：生成或编译达到 1800 s 时立即终止该步骤及子进程，记 `TIMEOUT_KILLED` 并隔离未完成产物。仿真达到预选可比较基线的 **1.5 倍**时立即终止进程树，记 `REGRESSION_KILLED`。基线有多次测量时，须在运行前选定比较值。触发上述限制后否定当前尝试并记录，不得继续该次性能实验；修正实现后在同一节点内重新验证。

退出非零、崩溃、断言失败、difftest mismatch 或输入不一致均记 `INVALID`；运行中确认失败即终止。失败或部分结果只能用于原因分析，不能作为性能收益证据。

比较须保持机器、输入、CPU 绑定、cycle 范围和 waveform/trace 设置一致。配置未变时复用已归档 gsim 测量，不重复运行；配置变化时重建相应基线，代码变化时建立相应 GrhSIM-IR 对照。gsim 的实测性能是追赶目标，不是固定时间门槛；采样/插桩时间不得混入未插桩性能基线。

性能收益的统一判定标准为 **6 次新旧交替复测**：在同一时间窗口内交替执行新、旧构建（3 新 + 3 旧，如 旧/新/旧/新/旧/新），运行前预注册执行顺序与比较口径，记录全部 6 次的 Host/墙钟时间、退出状态与等价性；任一构建出现 `INVALID` 运行时该次作废补跑，两组各保持 3 次有效。统计上报告两组均值、离散度与效应量，并按秩次判据确认显著性：全部 3 次新运行均优于全部 3 次旧运行（n=3+3 时等价于 Mann-Whitney U 单侧 p≤0.05）方可判定提升真实；交错后仍无法与噪声区分时须加跑或改进机制，不得按 `ACCEPTED` 收尾。约 90 分钟尺度的机器漂移已达 ~4%，禁止用跨窗口或非交错历史数据充当对照。

## 文档与 Git 归档

每个节点维护一份 `pdocs/` 报告，逐阶段更新以下信息，节点结束时补齐[GrhSIM-IR CoreMark 实验索引](grhsim-ir-xiangshan-coremark.index.md)：

- 假设与机制：创新点、瓶颈证据、局部目标、改动范围及语义约束。
- 复现方法：基线 commit、工作区实验版本差异、输入身份、完整 Make 命令/参数/环境、资源、计时边界、重复次数和止损线。
- 结果与分析：各次时间、退出状态、等价性、变化比例及计算口径；相关代码规模/热点数据、噪声、限制和重试差异。
- 最终判定：`ACCEPTED` 的依据（含 6 次交替复测完整数据与统计分析）、被否定尝试的过程记录与未执行阶段、保留实现的最终 commit、后续方向及报告链接。

报告须自包含，将临时产物中的证据整理入正文，不能用“详见日志”、路径或哈希代替分析。正式引用只能指向已跟踪或随节点一起提交的文件；节点内可引用待归档的新文件。不得提交生成代码、日志、profile、波形和二进制，也不得强制添加生成目录。

**仅节点完成时集中暂存、检查并提交代码、测试、报告和索引，每个有改动的仓库原则上一次提交。** 涉及子模块时先提交子模块，再提交根仓库指针与档案。中间版本用基线及差异记录，不为取得 commit 而提前提交。

用户启动 `/goal` 已授权正常暂存和节点最终提交。复用已有 Git 授权，不按阶段、文件或仓库反复询问；环境确需额外权限时，完成全部准备后在节点收尾集中处理，不绕过权限限制。

每完成 3 个节点，在收尾时更新当前最佳、已排除方向和剩余主要差距，检查是否陷入低收益微调。整体累计验收门为：M0 固定基线；M1 热点证据及至少 3 个独立方向；M2 小规模等价和生成/编译门槛；M3 完整 100k 验证；M4 最终性能经 6 次新旧交替复测统计确认为真实提升、归档全部达标。只有 M4 完成才能关闭整个 goal。

## 当前 100k 实测性能

自 2026-09-14 起，CoreMark 性能测量统一执行 **100,000 cycles**，以降低短窗口噪声。以下为同一主机、输入和仿真配置的 gsim 归档测量及最新 GrhSIM-IR 正式结果；gsim 不在节点交替测量窗口内。

- 配置：`testcase/xiangshan/ready-to-run/coremark-2-iteration.bin`，`XS_NUM_CORES=1`，`XS_EMU_THREADS=1`，`XS_EMU_CPU=2`，waveform/commit/RAM trace 关闭，cycle 上限 `100000`。
- **gsim**（emu 编译于 2026-09-10 01:50:26）：Host time spent **46.965 s**；`instrCnt=238550`、`cycleCnt=99998`、IPC **2.385548**、末端 PC `0x80000b40`、guest cycles **100001**，退出码 0，DIFFTEST 无 mismatch。
- **GrhSIM-IR**（[NO00050 fanout 发布批量 OR](NO00050-grhsim-ir-fanout-bulk-or-20260921.md)，编译于 2026-09-21）：正式第三轮三次 new Host 为 **69.666 / 69.440 / 69.449 s**，均值 **69.518333 s**，样本 SD **0.127962 s**；相对同窗口 old 均值 **70.152000 s** 降低 **0.9033%**；第二轮（另一窗口）old 均值 69.894667 / new 均值 **69.373000 s**，降低 **0.7464%**。两轮均 max(new) < min(old)（第三轮 69.666 < 69.995，第二轮 69.544 < 69.751），U=0、单侧精确 p=0.05；第一轮秩次门未过（噪声窗口）按协议加跑后连续两轮通过。九次正式运行均为 `instrCnt=240349`、`cycleCnt=99996`、IPC **2.403586**、末端 PC `0x80000c0c`、guest cycles **100001**，退出码 0，DIFFTEST 无 mismatch。
- 按最新第三轮 new 均值计算，GrhSIM-IR 为 gsim 归档时间的 **1.480239×**，高 **22.553333 s（48.02%）**；该比例表示剩余目标差距，不代替同窗 old/new 统计。NO00050 机制：compute 侧 fanout 发布（`activate()`/`armPorts()`）按对齐 8 字节窗口归并为单条 memcpy 形式 u64 批量 OR（无分支，gsim 形态对齐），`fanout_bulk_groups=58,427`/`bytes=181,932`；完整生成 **730.83 s**、fresh 编译 **202.05 s**（nproc=32），HDLBits DUT=001 回归通过，正式与筛选 model md5 全等。本节点同时否决：P4 表达式树状发射（clang 22 微实验六种体形态净差 ≈0，cpu_local 已全提升）、P3 `_BitInt` 宽值（剩余池 <0.5%）、memWrite 端口 arming（单写者 op 覆盖 3.2%，两票不一致）、粗粒度 supernode 256（−9.86%，粒度旋钮双向关闭）。节点 NO00050 已完成，本次不开始下一节点。
- **NO00046 / NO00047 均为失败节点**（代码全部回退）：NO00046 topocut 普查证伪五个发射层候选；[NO00047](NO00047-grhsim-ir-materialization-20260920.md) 微结构解剖后物化门三变体回归（clang 控制流拆分惩罚）、lean write 正式幅度不足，经用户人工判定 FAILED（2026-09-20）。**两波直线化路线已于 2026-09-20 由用户决定放弃**。**粒度旋钮（max-op）双向关闭（NO00036 细 −30.69% / NO00050 粗 −9.86%，128 为局部最优）**。下一节点候选（按 NO00050 移交）：boundary 写回层（changed 比较+存储，compute 语句 16.4%）的批量/位打包；commit 侧 write_cell 调用形态（~93 cyc/端口评估，NO00047 归因）；compute 71% 零产出激活池仍受 clang 控制流惩罚封锁。

上述 gsim 归档和 IR 实测分别使用 Makefile 目标 `run_xs_gsim_emu` 和 `run_xs_wolf_grhsim_ir_emu`，均设置 `XS_SIM_MAX_CYCLE=100000`、`XS_NUM_CORES=1`、`XS_EMU_THREADS=1`、`XS_EMU_CPU=2`、`XS_WAVEFORM=0`、`XS_COMMIT_TRACE=0`、`XS_RAM_TRACE=0`。

## 报告索引

报告索引已分离为独立文件 [GrhSIM-IR CoreMark 实验索引](grhsim-ir-xiangshan-coremark.index.md)，按完整节点登记并维护 `NOxxxxx` 编号；节点结束时随归档一并更新。
