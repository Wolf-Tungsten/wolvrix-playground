# 时钟域制导的调度方案

## 1. 目标

为后续仿真器准备一套静态调度计划，使运行时 `eval()` 不必全图重算，而是只执行本拍必需的操作。

运行时模型仍然对齐 Verilator 风格：

```cpp
void eval();
```

但 `eval()` 内部不再以“模块/always block”为单位调度，而是以预先构建好的 `CDAGroup` 为单位调度。

核心优化目标：

- output 相关逻辑始终可得
- 事件判定逻辑先算
- 只有对应事件真正触发时，才计算该事件组下的 reg/mem 写口数据逻辑锥
- 多个事件组共享的公共逻辑只计算一次

