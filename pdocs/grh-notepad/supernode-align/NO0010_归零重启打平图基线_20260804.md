# NO0010 归零重启：打平图基线与 ir-scale 收官复现

日期：2026-08-04
前置：用户裁定（2026-08-04）：对齐对象应为 gsim **打平图**（生产图有表达式树折叠，无意义）；且一切研究须在 ir-scale 收官的 2,825,239 可复现的基础上进行。本文替代 NO0002 成为本专题的新基线；NO0002–NO0008 中基于 gsim 生产图的数字一律标记为"旧基线"，仅作过程记录。

## 0. 对齐目标（用户裁定，2026-08-04）

本专题的对齐目标明确为：**grhsim AM 的 compute 图与 gsim 打平后图，在各自完成超节点划分后、同等超节点数量的前提下，跨超节点边数（cross_values）的差距**（当前达标线 ≤1.10x，即 §2 的 212,499）。

对齐**不是**照抄 gsim 的划分规则：两侧图的分布不同，AM 侧要追平同一 cross_values 水平，需要**比 gsim 更激进的规则、更多的 pass** 来实现相同效果。因此后续所有工作的评判标准是"在同等超节点数约束下 cross_values 差距是否收敛"，而不是"规则/调度是否与 gsim 一致"。

## 1. 口径清算（T5 主口径的确认）

ir-scale 的 "compute" 不是 GRH post-stats 的 `top_compute_ops`（那是 3.18M 的另一口径），而是 **T5 口径**（ir-scale NO0008 §2）：指令图上 **logic + mux + concat + slice + cmp + arith + shift + cast** 八类 op 的节点数。

复现验证（桶映射用 `scripts/compare_ir_shapes.py` 的 GRH/GSIM 映射）：

| 侧 | 实测 T5 compute | 文档值 | 偏差 |
|---|---:|---:|---:|
| gsim 打平图（20260804 重导） | **2,813,531** | 2,813,531 | **0（精确命中，验证桶定义）** |
| AM（5335696 收官提交重建） | **2,827,613** | 2,825,239（1.0042x） | +2,374（0.08%，1.0050x） |

AM 侧的 0.08% 漂移源于收官当次构建与 5335696 提交本身的微小差异；在基线容差内。复现配方：

```bash
# worktree at 5335696（/tmp/wolvrix-prev），python 包换装为该 worktree
WOLVRIX_XS_GRHSIM_LANE_AGGREGATE=on WOLVRIX_XS_GRHSIM_LANE_AGGREGATE_ARRAY=on \
WOLVRIX_XS_GRHSIM_LANE_AGGREGATE_MIN_LANES=4 WOLVRIX_XS_GRHSIM_COMB_LANE_PACK_ARRAY=on \
WOLVRIX_XS_GRHSIM_ENABLE_STATS=1 WOLVRIX_XS_GRHSIM_STOP_AFTER_PRE_SCHED=1 ... \
.venv/bin/python3 scripts/wolvrix_xs_grhsim.py build/xs/wolf/wolf_emit/xs_wolf.f SimTop <normalize_dir> "" <read_args> info --waveform off --perf off
# 然后 worktree 的 grhsim-am-lower-json 带两个 JSONL 导出环境变量
```

注意：当前 HEAD（ba0a69a，kMemory 改名）用同一配方得到 3,177,299（管线自检）/3,183,351（5335696），都**不是** ir-scale 口径的 2,825,239——之前 NO0002 勘误中"无法复现"的迷思由此解开：是口径找错了（post-stats 汇总口径 ≠ T5 指令图口径）。

## 2. 新基线（2026-08-04，全部重测）

| | gsim 打平图 | AM（收官 5335696，rotation） | AM（5335696 + 相位式） |
|---|---:|---:|---:|
| 数据集 | `xs_gsim_flat_prod_20260804` | `xs_am_flat_final_20260804` | `xs_am_flat_final_seq_20260804` |
| instructions | 3,043,902 | 3,371,971 | 3,371,971 |
| def_use 边 | 4,340,605 | 5,760,341 | 5,760,341 |
| 超节点数 | 88,375 | 25,550 | 30,174 |
| **cross_values** | **193,181** | 1,476,578 | **764,523** |
| ├ compute 消费方 | 193,181 | 1,288,367 | 574,596 |
| **比值** | 1.0 | **7.64x** | **3.96x** |
| 达标线（≤1.10x） | 212,499 | — | — |

- gsim 打平图的超节点由 gsim 自己在打平图上 coarsen+DP 产生（`--flatten-nodes --supernode-max-size=15`）。
- AM 两列共用同一张指令图（同一 normalize/lower-json 输入），仅 coarsen 调度不同；块数约束（≤88,375）两列均满足。
- 相位式改动（`activity_schedule.cpp`，与主仓 ba0a69a 上的版本逐字节相同）在新基线上重估成立：**7.64x → 3.96x（-48.2%）**，与旧基线上的 8.10x→4.01x 一致。

## 3. 旧基线对照（仅存档）

| 口径 | rotation | sequential |
|---|---:|---:|
| 旧基线（gsim 生产图 84,786 超节点 / 185,716） | 8.10x | 4.01x |
| **新基线（gsim 打平图 88,375 / 193,181）** | **7.64x** | **3.96x** |

两侧数字同向变化，旧结论的机制归因（轮询破坏 Out1 链、fanout≥2 是天花板、commit 结构性）在新基线上仍需复核——优先复核 fanout 结构与 182,810 commit 项在打平图口径下的对应关系（后续文档）。

## 4. 数据与产物登记

- 新数据集：`topo-partition-proj/exp/dataset/xs_gsim_flat_prod_20260804/`（gsim 打平图 + coarsen/dp 两段 assignment）、`xs_am_flat_final_20260804/`（收官代码 rotation）、`xs_am_flat_final_seq_20260804/`（收官代码+相位式）。
- 收官复现环境：`/tmp/wolvrix-prev`（worktree at 5335696，含构建产物与换装后的 python 包；主仓包已恢复）。
- 测量脚本不变：`scripts/supernode_align_metrics.py`（任意 topo-proj 图通用）。

## 5. 下一步

1. 新基线上的剩余差距归因（764,523 → 212,499）：fanout≥2 结构对照、commit 项复核。
2. 相位式改动的 difftest 门控需要在收官基线上重跑（旧基线的通过记录作废）。
3. 图形级 pass（alias 消除等）在新基线上重新评估收益。

## 增量更新 2026-08-04：复现配置固化为默认

用户裁定：复现成立的配置作为默认配置。落实核对：

1. **主仓 HEAD（ba0a69a）≡ ir-scale 收官（5335696）**：用收官配置在 HEAD 重建的指令图与 5335696 worktree 的逐字节一致（3,371,971 条指令，T5 桶逐项相同）。kMemory 改名只改了 post-stats 汇总行的分类标签，没动管线产物——**后续工作无需 worktree，主仓 HEAD 即为收官基线**。
2. **管线默认即收官配置**：`scripts/wolvrix_xs_grhsim.py` 的 `LANE_AGGREGATE=on / LANE_AGGREGATE_ARRAY=on / MIN_LANES=4 / COMB_LANE_PACK_ARRAY=on` 均为默认（NO0021 已翻转），无需显式 env。
3. **相位式 coarsen 即默认调度**：`activity_schedule.cpp` 的相位式循环在主仓工作树中无条件生效（未提交 git，提交时机由用户定）。
4. **测量默认即新基线**：`scripts/supernode_align_metrics.py` 无参运行 = gsim 打平图（`xs_gsim_flat_prod_20260804`）vs AM 收官+相位式（`xs_am_flat_final_seq_20260804`）的正式对比，当前读数 **3.9575x**（764,523 / 193,181；块数 30,174 ≤ 88,375）。
