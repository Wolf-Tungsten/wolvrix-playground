# Proposal r001/t0/s08（Φ 自动生成）

- task: grhsim-am-coremark  run: r001  trajectory: t0  step: 8/8
- 轨迹主线分支: `tes/r001/t0/main`  best: {"eval_id": "e00040", "score": -194792}
- 基线 am: score -273103（host 中位 273103 ms，eval e00001）
- 基线 gsim: score -24688（host 中位 24688 ms，eval e00002）

---

## 任务指令（x0）

# TES 任务指令（x0）：grhsim-am xiangshan coremark 50k 性能优化

这是每个 proposal 都会携带的常驻任务指令。它定义目标、约束与裁决口径，不随实验进展改变；如需修改，修改即构成新的实验条件，必须在 `tes/grhsim-am-coremark/state/insights.md` 追加记录。

## 目标

最小化 grhsim-am emu 仿真 xiangshan coremark 50k 周期（`-C 50000`）的 **Host wall time**（3 rep 中位，`taskset` 绑核，串行无干扰），直至**不劣于 gsim 同等负载**（同一 coremark bin、同一 difftest 协议、同一机器、同一测量协议测得的 gsim emu Host 中位）。

- 分数：`score = -median_host_ms`（越高越好）。
- 进度指标：`ratio = am_median / gsim_median`，目标 `ratio <= 1.0`。
- 参考基线在 run-init 时实测并冻结进 `tes/grhsim-am-coremark/runs/<run>/manifest.json`。

## 硬约束（任一违反即判失败候选）

1. **功能门**：difftest 逐位一致——每 rep 进程退出码 0 且 `instrCnt = 73,580`、`cycleCnt = 49,996`（coremark-2-iteration 50k 窗金标）。
2. **回归门**：`ctest -R grhsim` 全绿（emit-cost 系列确立的 17 项）。
3. **测量纪律**：计时 rep 串行、绑核、无构建等并发负载；计数/计时分离（正式计时不开 `EMU_RUNTIME_PROFILE` / `EMU_AM_BLOCK_EXECS` 等插桩）。
4. **编译预算**：从 cmake 到 emu 二进制就绪的累计墙钟 ≤ **40 min**，超预算即判 `compile_timeout` 失败，不进入计时——生成代码体积/单 TU 复杂度也是成本（NO0007 曾单 TU >40min），病态膨胀的候选没有资格谈运行时。

## 工作面

候选解 = wolvrix 仓库的一个 commit（在 tes 候选分支上）+ 可选的 emit 参数覆盖（`emit_args`，如 `--blocks-per-source`、`--dp-coarsen-*`、`--max-atoms-per-block` 等旋钮）。评估输入固定为 gsim 导出的同一张 exec-GRH（见 manifest 指纹），保证跨候选可控对比。

## 优化哲学（变更面纪律）

1. **GRH IR 冻结**：候选不得改变 GRH IR 的定义与语义（`wolvrix/include/core/grh.h`、`wolvrix/lib/core/grh.cpp` 等 GRH 数据结构与算子语义），也不得以 ingest 侧行为变化作为收益来源——GRH 是跨工具的稳定契约，冻结它以保证候选间可比、可归因。
2. **grhsim AM IR 可变**：AM IR（lower 之后的块/atom/调度结构）是主要优化面，允许灵活调整——新增或修改 AM IR 的结构与变换规则均合法。
3. **优化显式化为 pass**：优化手段应尽量体现为**显式的 grhsim AM 优化 pass**——有名字、可独立开关、能在 pipeline 中定位，便于单变量归因与回撤；避免把变换隐式揉进 emit/lower 的边角逻辑里。
4. **emit 规则变更须配文档**：确需改变 emit 规则的候选，应尽可能同步提供或更新文档（`wolvrix/docs/grhsim/`，如 `grhsim-am-pipeline.md`），说明规则变化与动机；文档随候选 commit 一并提交。

## 已知机制背景（来自 pdocs/grh-notepad/emit-cost 等，run-init 时的起点知识）

- Host 465.8s → 324.0s 的修复链：巨 atom 常量折叠（NO0011）、动态基座元素级发射（NO0012）、装配锥窗口化（NO0013）、dynblend 锥塌缩（NO0014）、窄值标量化（NO0016）、宽状态切片内联 + 掩码写展开（NO0018）。
- 当前残余成本画像（NO0018 收口）：跨 chunk uint64 数组槽 store→load 往返、状态 gather（194MB 模型对象上的值散布）、2.87x instr/atom 适配胶；匹配对加权 4.14x、>100x 尾部 13 对 / 1.5G。
- 已证伪方向：分区形状（NO0002）、存储宽度本身（NO0016 Stage A）；已回撤负优化先例：独立 concat 去零换 replace（NO0013 F2）。
- 测量陷阱先例：`-j32` 构建负载污染计时（supernode-align NO0017）、ASLR/PIE 布局效应（NO0338+）、跨构建 id 漂移导致 join 错配（emit-cost NO0017）。


## 评估协议

# 评估协议（grhsim-am-coremark）

Φ 会把本文件原样内联进每个 proposal 的「评估协议」一节。改协议即改实验条件：
run 期间不修改，修改应在 insights.md 追加记录。

- 负载：`coremark-2-iteration.bin`，`-C 50000`（50k 周期窗），difftest 参考
  `riscv64-nemu-interpreter-so`。
- 计时：3 rep 取中位（CV>5% 自动加测至 5 rep），`taskset` 绑核（core 见 config
  `eval.core`），串行无干扰（全局 LOCK + emu 进程守卫）；不开任何 profile 插桩。
- 功能门：每 rep 退出码 0、nemu 在线逐指令核对无 mismatch，且 instrCnt/cycleCnt 落在
  金标窗内（中心 73,584/49,998，容差 ±16/±8——窗口覆盖 gsim 与 am 两种 emu 在 50k
  周期窗停止点上的确定性小差，见 config `eval.golden_tol`）。
- 回归门：`ctest -R grhsim` 全绿。
- 编译预算：cmake→emu 二进制就绪累计 ≤ 2400s，超预算判 `compile_timeout`。
- 流水线：wolvrix 全量构建（Release + ccache）→ ctest → `grhsim-am-lower-json
  <exec_json> SimTop --schedule --emit <dir> --block-chunk-instructions 3000`
  → difftest emu 构建 → 计时 reps。
- 可调旋钮（候选可覆盖）：`--emit-args` 透传给 lower-json，如 `--blocks-per-source`、
  `--max-atoms-per-block`、`--tree-atom-fold-max-instr`、`--dp-coarsen-*`、
  `--merge-when-min-group`、`--block-chunk-instructions` 等（取值语义见
  `wolvrix/docs/grhsim/grhsim-am-pipeline.md`）。


## Φ 选中的历史节点（本轮 refinement 的出发材料）

### e00040（step 7, score -194792, host 中位 194792 ms）
- 假设: t0 主线 5 个窄标量 helper（slice_value/shift_left/shift_right/arithmetic_shift_right/signed_value，静态 534K 站、动态 22.5 亿次 outlined 调用）移入生成头文件 constexpr，若跨 TU 调用边界+常量传播阻断是一阶适配成本，Host 中位较 e00033 降 >=3%；<1.5% 则非一阶
- 洞察: 确认：194.792s（196006/194290/194792，CV 0.45%），较 e00033 降 10.02%，远超 3% 假设门；17/17 ctest 与 3 rep difftest 全过，compile_s=603.3s（emu_build 264.7s 无膨胀）；窄标量 slice/移位 helper 调用边界是 t0 当前最大一阶适配胶
- 分支 `tes/r001/t0/s07-c2`  commit `622a552a637b`  结果 `build/tes/grhsim-am-coremark/evals/e00040/result.json`

### e00027（step 5, score -222654, host 中位 222654 ms）
- 假设: 在e00022的source-word activity guard上叠加wide-storage first-touch，若扫描剪枝后仍保留宽状态locality余量，可使Host中位相对e00022再降>=2%；<1%则word guard已吸收主要布局收益
- 洞察: 确认：222.654s（CV 0.22%，较e00022的230.568s降低3.43%），17/17 ctest与3 rep difftest全过，compile_s=606.3s；word guard与first-touch在更细扫描层级仍可加，刷新run best
- 分支 `tes/r001/t0/s05-c1`  commit `8dfd73e7b16b`  结果 `build/tes/grhsim-am-coremark/evals/e00027/result.json`

### e00015（step 3, score -247458, host 中位 247458 ms）
- 假设: 精确 64-bit source-part activity 区间守卫可跳过静默 part 的函数调用与逐 byte 扫描，使 Host 中位相对 e00010 降低至少 3%，且 difftest/ctest 全过
- 洞察: 确认：247.458s（CV 0.82%，相对 e00010 的 273.258s 降低 9.44%），17 项 ctest 与 3 rep difftest 全过；静默 source-part 的 64-bit 预检收回了显著块间扫描开销
- 分支 `tes/r001/t0/s03-c1`  commit `96b8df23e0d3`  结果 `build/tes/grhsim-am-coremark/evals/e00015/result.json`

### e00001（step 0, score -273103, host 中位 273103 ms）
- 假设: am baseline
- 洞察: AM 基线（y0, a88e7a2）：3-rep 中位 273.1s（reps 274.3/272.2/273.1，CV 0.39%），compile_s 约 21min（冷 ccache 首轮放宽 90min 预算内）
- 分支 `None`  commit `a88e7a234b7f`  结果 `build/tes/grhsim-am-coremark/evals/e00001/result.json`


## 本轨迹已评估但未中选的变体（避免原样重试；可在其基础上改机制）

- e00003 s1c1: score -279209（279209 ms）— chunk 3000->12000 使更多窄值 chunk 内标量化、减少跨 chunk uint64 槽 store->load 往返，Host 中位应降 >=3%
- e00009 s2c1: score -274422（274422 ms）— init()字面量0死store发射期消除后：runtime.o编译瓶颈移除（全程compile_s降≥30%），Host中位变化<±0.5%（运行时中性）
- e00016 s3c2: score -257443（257443 ms）— 按调度块首次触及顺序重排宽 BitVector/Array 存储并叠加 init-zero-elision，可通过收缩活跃状态跨度和页足迹使 Host 中位相对 e00010 至少改善 3%，同时保持全部功能门；若不成立则关闭布局轴
- e00021 s4c1: score -239354（239354 ms）— source-part guard消除空part扫描后，wide first-touch仍可通过收缩实际执行块的宽态跨度/page足迹独立改善locality；叠加到e00015使Host中位再降≥3%；<1.5%则两机制成本重叠或布局收益不可加
- e00028 s5c2: score -235610（235610 ms）— e00022 的 source-word guard 仍逐 byte 从 activeWords_ 载入/清除活动位；按 source part 将 owned word 一次快照到局部 wordFlags，并将同 word 未扫描的前向激活留在局部，可减少全局位图访问并较 e00022 再降至少 2%；<1% 则 byte 快照/全局位图访存不是一阶成本。
- e00034 s6c2: score -229850（229850 ms）— 窄标量 v<K> 成员（1,263,224 个/约 10.1MB）按 scheduled Block 首触排序压实热点成员区，若窄态 gather 是一阶成本，Host 中位较 e00027 降 >=2%；<1% 则窄态 gather 非一阶、布局轴在窄态类关闭
- e00039 s7c1: score -212386（212386 ms）— 残余跨 word 拼接（静态 56,759 站、动态 6.89 亿次 outlined 调用）按对齐满字直存/窄跨 word 双语句/≤8word 宽 unroll 三形内联，若调用边界+逐迭代边界检查是一阶成本，Host 中位较 e00033 降 >=2%；<1% 则拼接调用边界已非一阶

## 本 step 任务

设计并实现 **2 个互不相同的候选**（机制层面不同，不是同一想法的参数微调；任务支持的可调旋钮见上方任务指令）。每个候选：在各自 worktree 实施 → 提交到各自候选分支 → 任务 evaluator 串行评估 → `tesctl.py record-eval` 登记。全部完成后 `tesctl.py finish-step` 裁决。

## Φ 选择调试表（RPUCG）

| node | score | U | ρ | n | RPUCG | selected |
|---|---|---|---|---|---|---|
| e00040 | -194792 | 1.000 | 1.00 | 0 | 4.000 | * |
| e00033 | -216481 | 0.900 | 0.86 | 1 | 2.186 |  |
| e00027 | -222654 | 0.810 | 0.71 | 1 | 1.881 | * |
| e00022 | -230568 | 0.900 | 0.57 | 2 | 1.471 |  |
| e00015 | -247458 | 0.810 | 0.43 | 2 | 1.239 | * |
| e00004 | -271095 | 0.900 | 0.29 | 4 | 1.071 |  |
| e00001 | -273103 | 0.810 | 0.14 | 9 | 0.853 | * |
| e00010 | -273258 | 0.729 | 0.00 | 1 | 0.729 |  |
