# A0086 recon：r004/t0/s00 e00085 动态热点画像（2026-08-23）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t0**；
被画像 tip：AM 基线 **e00085**（commit `1563c3d837fc`，完整 12 开关生产表型）。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00085 \
  --out build/tes/grhsim-am-coremark/recon/r004-t0-s00
```

recon 以 e00085 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本持有全局 `build/tes/LOCK`，运行期间
未并发任何构建或仿真负载。完整流程墙钟 **1016.6s**。profile run 得到
`instrCnt/cycleCnt=73580/49996`，与 AM 金标精确一致，difftest 门通过。产物位于
`build/tes/grhsim-am-coremark/recon/r004-t0-s00/`（`report.md`、`report.json`、
`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 Host 时间为 227.603s；它包含逐块 `rdtsc` 和计数开销，不是正式计时，
不得与 e00085 的 193.403s 基线相减或写入 score。

## 动态总账

| 项目 | 动态结果 | 解读 |
|---|---:|---|
| eval calls / rounds | 100,102 / 200,154 | **1.9995 rounds/eval**，调度轮数稳定 |
| compute | **166.733s / 73.4435%** | 596,466,270 次 compute-block 执行 |
| commit | **60.234s / 26.5324%** | 15,732,466 次 commit-block 执行 |
| other | 0.055s / 0.0241% | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | backward 是 forward 的约 1.184x |
| changed marks / clears | 200,264 / 200,264 | 成对出现，约 2.0006/eval |
| blocks / block cycles | 93,601 / 876,687,574,636 | 以下占比均以总块 cycles 为分母 |

commit block 仅占全部 block 执行的约 **2.57%**，却占阶段墙钟 **26.53%**；平均每次
commit-block 执行约为 compute-block 的 **13.7x**。因此 commit 仍是高单次成本池，
但 compute 的 73.44% 总量不能被 commit 局部优化替代。

热点集中度为：top-2 **8.620%**、top-5 **14.535%**、top-10 **18.744%**、top-20
**25.384%**、top-50 **39.784%**。top-50 中 33 个 commit 块占总 cycles 26.431%，
17 个 compute 块占 13.353%；其外仍有 **60.216%** 长尾，单站点微调的收益上限很低。

## 最高权重块

| rank | block | role / 静态形态 | execs | cycles | cycles% |
|---:|---:|---|---:|---:|---:|
| 1 | 93159 | commit，8,464 atoms/instrs | 100,103 | 50,377,211,003 | **5.746%** |
| 2 | 93141 | commit，12,189 atoms/instrs | 100,102 | 25,199,137,821 | **2.874%** |
| 3 | 90657 | compute，7,003 atoms / 7,068 instrs | 100,791 | 22,092,229,288 | **2.520%** |
| 4 | 90656 | compute，7,001 atoms / 7,757 instrs | 88,260 | 18,963,085,296 | **2.163%** |
| 5 | 83835 | compute，7,003 atoms / 8,927 instrs | 50,000 | 10,800,522,355 | **1.232%** |
| 6 | 93085 | compute，6,217 atoms / 8,125 instrs | 100,053 | 10,158,748,137 | **1.159%** |

- b93159/b93141 合计 **8.620%**，仍是 commit 主峰。生成形态与 r002 归因一致：
  b93159 含 1,313 个宽站、870 个窄写站和 559 个 detect group，属于大规模数据写回池；
  b93141 是更大的窄站写口阵列。旧 TES 已实测 `commit-write-branchless` 和行 RMW 合并
  静态成立、Host 为负，故本权重不能作为原样重试这两种机制的依据。
- b90656/b90657 合计 **4.683%**，仍是最大的连贯 compute 池。它们是
  `sys-task-body-outline` 后残留的 task fire/条件评估块；冷 task body 已移出，当前
  动态权重支持研究谓词求值本身，而不是再次 outline 冷体。
- b83835/b93085 合计 **2.391%**，生成代码仍以窄标量比较/归约树及宽值 gather 为主。
  两块每次执行成本高，但池上限小于 task 守卫池；候选必须是覆盖整个同构池的 pass，
  不能只改一个生成站点。

## 裁决与下一步建议

本 recon 成功刷新 r004/t0 的动态证据，功能门通过且不消耗 eval-id。r004/t0/s01 的
K=2 局部采样建议围绕两个机制互异的池展开：

1. 对 b90656/b90657 的重复 task fire 条件做跨 atom 的公共谓词归并或分层短路，保持
   已 outline 的 task body 不变；以该池 cycles 至少下降 20% 且整体 Host 越过噪声门
   作为可证伪预期。
2. 对 b83835/b93085 同构比较/归约与 gather 做池级专化，先以生成 engagement 证明覆盖
   两块及同族，再要求块 cycles 明显下降；2.391% 的已知上限意味着不能承诺大跨度收益。

commit 池只有在提出区别于 branchless/行 RMW 的数据布局或批量写回机制，并能解释
`b93159/b93141` 的数据侧成本时才可重开。top-50 外 60.216% 的长尾要求优先选择
池级 pass，不以局部源码行数或静态站点数替代动态权重。本 action 只完成 recon，不启动
后续 step。`record-recon` 登记后，状态机下一 action 为 **r004/t0/s01 step（K=2）**。
