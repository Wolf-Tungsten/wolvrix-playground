# A0123 step：r004/t4/s04 宽 mux 确认与 commit 清零弱正（2026-08-25）

对应当前 `next` = `step`，轨迹 `t4`，step 4/4，K=2。Phi 选择 e00119（Host
**173.624s**）与 e00085；最新动态证据来自
`build/tes/grhsim-am-coremark/recon/r004-t4-s02/report.md`（基于 e00108）：
b69157/b69158/b69159 宽 mux 池占 **1.563%** block cycles，b93159/b93141 commit
双峰占 **8.569%**。本 step 分别检验已确认宽 mux 融合在 e00119 基座上的迁移
可加性，以及本轨迹 e00107 lazy commit scratch 弱正机制与 e00119 的组合可加性。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--array-broadcast-mux-chain-fuse` | e00131 / `72405b7` | e00119 + 迁移源 e00106 -> 宽 mux 池 **1.563%**，e00106 已确认 4 chains/92 steps 融合 -> 在 predicate-run + pow2-index tip 上消除宽 broadcast/mux 中间槽物化 -> Host 至少改善 1% | **166.837s**（166.837/166.234/170.533s，CV 1.39%，单簇、非 noisy），`compile_s=645.6s`；17/17 ctest、三次 difftest 全过，生产命中 4 chains/92 steps | 较父改善 **3.91%**，越过 3% 确认带；`migration_source=e00106`、outcome=`win`，合入 t4/main |
| c2 `--commit-scratch-lazy-init` | e00132 / `fddb7f3` | e00119 + 本轨迹 e00107 -> commit 双峰 **8.569%**，e00107 曾有 1.84% Host 信号 -> 把 chunked commit scratch 清零后延到事件门打开后 -> Host 至少改善 1% | **172.265s**（173.122/170.694/172.265s，CV 0.72%，单簇、非 noisy），`compile_s=637.2s`；17/17 ctest、三次 difftest 全过 | 较父改善 **0.78%**，未达到 1% 假设且在 3% 确认带内；不合入 |

两个正式结果均显式传入 e00119 的完整 14 项生产表型，再分别增加候选开关；
`result.json.emit_args` 与各自 `tes-candidate.json` 通过硬审计。评估严格串行，e00131
完成并登记后才启动 e00132，二者均通过功能门且没有 interference 或 noisy 标记。

## 裁决与机制分析

- `finish-step` 选择 e00131，并将 `72405b7` 快移到 `tes/r004/t4/main`。e00131
  比 e00119 快 **6.787s（3.91%）**，使 t4 确认 best 更新为 **166.837s**；全局
  raw best 仍为 t1/e00125 **164.729s**。
- 宽 mux 融合在 predicate-run + pow2-index 基座上重新越过确认带。其 4-chain/92-step
  静态覆盖未变，但本次增量强于此前若干基座上的 1.31%-2.81% 弱正，确认该机制可加，
  同时再次表明收益受代码布局和基座组合影响。没有 e00131 post-change recon，不能在
  本 action 宣称 1.563% 池的具体 cycles 降幅。
- lazy commit scratch 在 e00107 单独测得 1.84% 信号，在 e00119 上只剩 0.78%，未达
  预注册下限；结合其他基座上的弱正或回退，清零后延应降级为布局敏感的次要机制，不能
  视为击穿 8.569% commit 双峰。若重开，应先动态拆分 commit 清零、scan、write 与
  detector 成本，而不是继续组合相同开关。

## 对下一步的建议

t4 已完成 4/4，预算达到 **48/48**。保留 e00131 作为确认 winner；后续 proposal
可把宽 mux 视为可复用机制，但需继续以动态池覆盖判断迁移价值。状态机下一 action 为
**r004/t5/s04 step（K=2）**；本 action 只作预告，未启动它。
