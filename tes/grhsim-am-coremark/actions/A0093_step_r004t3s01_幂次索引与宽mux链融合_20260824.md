# A0093 step：r004/t3/s01 幂次索引与宽 mux 链融合（2026-08-24）

对应当前 `next` = `step-resume`，轨迹 `t3`，step 1/4，K=2。`begin-step` 与
c1/e00093 已在恢复前完成；本次只补做 pending `[2]`，没有重复 begin-step 或重跑
已登记候选。Phi 唯一来源为 r004 AM 基线 e00085（commit `1563c3d837fc`，Host
**193.403s**，完整 12 开关表型）；两个候选分别处理 A0092 recon 中占总块 cycles
**3.140%** 的幂次深度 memory-read 池和 **1.567%** 的宽 broadcast-to-mux 流扫池，
机制互异。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--memory-read-pow2-index` | e00093 / `3abdf54` | e00085 -> b9029/b9041/b9047/b9062/b9063/b9089 合计执行 158,668 次、占总块 cycles **3.140%**，六块各含 512 个 `index_words(..., 9, 512)` 与不可达越界判断 -> 对索引位宽精确覆盖 2 的幂深度的 memory read 直接提取并掩码窄标量索引 -> 六块池至少下降 25%、Host 至少下降 0.75% | **172.530s**（173.675/170.387/172.530s，CV 0.97%，单簇、非 noisy），`compile_s=642.2s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00085 名义 **-10.79%** | raw-score winner；生产六块的 3,072 个站点全部专化且无残留 `index_words`，Host 门通过并显著越过 3% 确认带；池级 25% 降幅仍待 post-change recon |
| c2 `--array-broadcast-mux-chain-fuse` | e00094 / `822afa2` | e00085 -> b69157/b69158/b69159 合计执行 149,997 次、占总块 cycles **1.567%**，22,528-bit 链逐层物化 352-word 中间数组 -> 精确融合相邻、sole-use、同块且至少 16 级的 64-bit lane `ArrayBroadcast -> ArrayMux` 链，只物化链尾 -> 三块池至少下降 70%、Host 至少下降 0.75% | **187.491s**（187.491/185.467/189.102s，CV 0.97%，单簇、非 noisy），`compile_s=638.3s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00085 名义 **-3.06%**，同窗较 c1 慢 **8.67%** | Host 门通过且刚越 3% 带，但 raw score 落败；生产只命中目标族的 4 链/92 steps（b69157=23、b69158=23、b69159=46），池级 70% 降幅仍待 post-change recon |

两个 `tes-candidate.json` 都声明相对冻结父表型的单一新增开关。正式 evaluator 的
`result.json.emit_args` 均为 12 项冻结父开关再追加各自候选开关，逐项审计一致；两次
评估严格串行，只通过任务 evaluator 完成全量构建、17 项回归、金标 difftest 和绑核
3 rep，编译时间均低于 2400s 门。

## 裁决与机制分析

- `finish-step` 按 raw score 选择 e00093，并将 commit `3abdf54` 快移到
  `tes/r004/t3/main`；t3 首个节点记为 outcome=`initial`。e00093 比 e00094 快
  **7.98%**（以 e00094 为分母），同时把全局 best 从 e00092 的 188.662s 刷新为
  **172.530s**。当前 AM/gsim = **7.594x**，预算 10/48。
- c1 删除的是六个动态热块内重复 3,072 次的通用多词索引 helper、范围判断和相关
  临时值；生产覆盖完整，Host 收益远超预注册 0.75% 门，是本 step 最强证据。不过
  10.79% 大于 recon 中该池的 3.140% 权重，说明插桩画像百分比只能定位病灶，不能当
  作严格 Amdahl 上界；代码体积、调用/分支和寄存器压力等连带效应尚未分离，不能在
  没有 post-change recon 的情况下宣称池级 25% 已验证。
- c2 的严格匹配规则避开可见中间值和跨块依赖，并确实消除了目标三块的 92 层宽数组
  物化；3.06% Host 改善支持该机制为正向，但其源池更小，且与 c1 的 8.67% raw 差距
  足够让本轮 winner 清晰。70% 池级目标尚无动态复测，当前不把落败分支直接叠加到
  t3 主线。

## 后续建议

t3 下次证据到期时优先对 e00093 做 post-change recon：核对六个 memory-read 热块的
cycles 权重、生成代码/调用消除与新的替代热点，再决定是否把幂次索引专化推广到其他
深度或与宽链融合组合。宽链方向只有在 b69157/b69158/b69159 的池级降幅得到量化后才
值得占用后续席位；静态命中数本身不作为追加依据。

状态机给出的下一 action 是 **t4/e00085 recon**；本 action 只完成 t3/s01，未启动
该 recon。
