# A0098 round-summary：r004 第 1 轮跨轨迹与基线重锚（2026-08-24）

对应 `next` = `round-summary`（round 1）。本 action 无新 eval-id、无构建开销，候选
数据来自 `state/ledger.jsonl` 的 r004/e00087-e00098；按 round 2 中段重锚协议，另以
既有 emu 串行执行 e00085/e00086 `retime`。冻结 ledger、manifest 和候选分数不回写。

## 本轮各轨迹 winner 与假设

| 轨迹 | step winner | Host / 相对冻结 AM e00085 | 假设与裁决 |
|---|---|---:|---|
| t0 | e00088 `--inline-unsigned-div64` | **189.829s / -1.85%** | b93085 的 1,252 个 unsigned-64 Div 改用零除保护的原生 `/`；全门通过，但未越 3% 确认带，只记弱正 initial |
| t1 | e00089 `--host-call-guard-cache` | **190.827s / -1.33%** | 6,314 个相邻 host-call 组复用完整 guard；全门通过，弱正 initial，尚无 post-change 热点降幅 |
| t2 | e00092 `--host-call-predicate-run` | **188.662s / -2.45%** | 6,314 个完全同谓词 run 合并分支，覆盖 12,827 个 host-call；全门通过，达到预注册 1% 门但未越确认带 |
| t3 | e00093 `--memory-read-pow2-index` | **172.530s / -10.79%** | 六个 512-depth 热块的 3,072 个通用多词索引改为精确 9-bit 掩码；全门通过并显著越过确认带，刷新全局 best |
| t4 | 无 | - | e00095 scratch 后延、e00096 task 谓词 run 均为 fixture 误报导致 `ctest_fail`；未进入生产计时，机制是未测而非性能证伪 |
| t5 | e00097 `--commit-scratch-after-gate` | **191.116s / -1.18%** | b93159/b93141 的大型 scratch 清零移入事件门；全门通过并达到 1% 下限，但未越确认带，池级降幅待 recon |

本轮 12 个候选消耗 12 个 eval 席位：9 个 `ok`、2 个 `ctest_fail`、1 个
`difftest_fail`。六轨迹均完成 s01；t4 按全失败预算语义原地消耗一步。当前总预算
**14/48**（含双基线），best_overall 为 t3/e00093、commit `3abdf544eb6e`。

## 双基线中段重锚

两个 retime 严格串行，只复用既有 emu 补正式计时，不占预算、不分配新 eval-id；每侧
三次 difftest 均通过，簇状态均为 unimodal。

| 基线 | run-init 冻结值 | round 1 retime | 漂移 | 裁决 |
|---|---:|---:|---:|---|
| AM e00085 | 193.403s（193.403/188.958/194.839s） | **194.922s**（194.922/192.927/196.263s，CV 0.86%） | **+0.79%** | 低于 3%，AM 水位稳定 |
| gsim e00086 | 22.720s（22.720/22.608/23.100s） | **23.780s**（24.353/23.186/23.780s，CV 2.45%） | **+4.67%** | 超过 3%，追加 insight 并重估 target 口径 |

冻结口径继续作为 r004 状态机与候选裁决的唯一口径：AM/gsim 基线为 **8.512x**，
e00093/gsim 为 **7.594x**。重锚只作当前机器水位诊断：AM/gsim 为 **8.197x**，
e00093/new-gsim 为 **7.255x**；相对 new-AM 的跨窗名义改善为 **11.49%**。gsim
单侧变慢而 AM 仅漂移 0.79%，不能把 target 放宽解释为 AM 机制收益，也不回写
`run.json`、manifest 或 ledger 中的冻结分数。

## 跨轨迹机制分析

- **确认收敛：幂次深度 memory-read 索引专化。** e00093 是本轮唯一远离 3% 噪声带
  的 winner，且功能门、回归门和生产覆盖完整。其 10.79% 收益大于旧 recon 中六块
  3.140% 的占比，下一次必须对 e00093 做 post-change recon，分解 helper、代码布局、
  分支与寄存器压力的连带效应，再决定向其他幂次深度推广。
- **弱收敛：host-call 谓词共享和 commit scratch 生命周期。** e00089/e00092 在同一
  task 双峰邻域给出 1.33%/2.45% 弱正信号；e00091/e00097 在 commit 双峰邻域给出
  2.25%/1.18% 弱正信号。重复方向一致，但全部缺 post-change 动态池降幅，且多数仍在
  3% 确认带内；round 2 应先 recon 各轨迹 tip，不把跨轨迹结果直接迁移或叠加。
- **发散或未决：Concat 位置装配与 fixture 契约。** e00090 的 scalar Concat 版本只有
  0.98% 名义改善；e00098 因 operand 位序实现错误在 guest cycle 2 失败，不能支持性能
  结论。t4 两个 fixture 误报使席位完全未测；未来重开须先修结构化测试契约，并使用新
  step/eval 完整评估，不能把失败生成物当作正向证据。
- **无需用户中途改方向。** C=6/L=4/K=2 与预算保持冻结。round 2 优先刷新各 initial
  tip 的 recon，按轨迹独立证据继续局部精修；唯一可优先扩展的一阶方向是 e00093，
  但仍须先验证 post-change 动态机制。

`round-summary-done --round 1` 与 `action-done` 登记后，状态机给出的下一 action 是
**r004/t0/s02 step（K=2）**。本 action 不调用 `begin-step`、recon、evaluator 或
任何下一步执行命令。
