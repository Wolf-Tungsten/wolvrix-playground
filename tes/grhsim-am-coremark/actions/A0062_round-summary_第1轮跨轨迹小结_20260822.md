# A0062 round-summary - r003 第 1 轮跨轨迹小结（2026-08-22）

对应 `next` = `round-summary`（round 1）。本 action 无评估开销；数据全部来自
`state/ledger.jsonl`（r003 e00051-e00056）以及 A0060/A0061 step 笔记。

## 本轮各轨迹 winner 与假设

| 轨迹 | step | winner | Host 中位 | vs e00051（名义） | CV | 假设（一句话） | 裁决 |
|---|---|---|---:|---:|---:|---|---|
| t0 | s01 | e00054 (c2) | **247.560s** | **-31.99%** | 0.00% | `--sys-task-body-outline` 将 7,235 个冷 `fwrite` system-task body 抽为 `noinline`，热块只保留 fire、实参物化和调用，预期至少降 4%，低于 2% 证伪 | **机械确认**；较 c1 快 26.03%，已入 `t0/main`（`b1f2c8d`） |
| t1 | s01 | e00056 (c2) | **241.956s** | **-33.53%** | 0.41% | `--wide-mux-chain-fuse` 融合 22528-bit broadcast-to-mux 相邻链，消除每级 352-word 中间数组，预期至少降 1.5%，低于 0.5% 证伪 | **机械确认**；较 c1 快 10.39%，已入 `t1/main`（`014c3ae`） |

落选候选也均为实质机制且通过全部功能门：t0 c1 e00053
`--scan-branch-hints` = 334.687s（名义 -8.05%，CV 0.00%）；t1 c1
`--activity-summary-scan` = 270.003s（名义 -25.82%，CV 0.00%）。四个候选均为
`status=ok`，17/17 ctest、3 rep difftest（73580/49996）全过，且
`compile_s` 处于 2400s 预算内。

## 跨轨迹对比

- **收敛：前端/数据流形态仍是两条轨迹的共同高价值方向。** t0 的 outline
  瘦身热块前端，t1 的 wide-mux 融合消除中间状态写读；两者都不改变 GRH IR，
  都通过显式 emit 规则改善生成代码的取指或访存形态。这与 r002 已确认的
  scan hints、task-body outline 和 wide-mux 同族证据一致，省指令/门控路线没有
  被本轮重新打开。
- **发散：命中的池不同，不能把机械排名当作机制强弱。** t0 的 e00054 与 t1 的
  e00056 分属不同计时窗口；绝对值只差 2.26%（241.956s vs 247.560s），不具备
  跨窗因果比较资格。t0 两候选的 1.352x 比值、e00051/e00055 的 1.348x 比值
  都落在历史已检出的 per-process 快慢态 1.3-1.4x 带内；批内低 CV 不能排除整批
  同态。因此 e00054/e00056 是有效的状态机 winner，但不能把 -31.99%、-33.53%
  或候选间差值全部归给对应机制。
- **可复用的因果尺度仍来自同窗证据。** r002 同输入同窗已经给出
  task-body outline 约 -5.91%、wide-mux-chain-fuse 约 -2.19%/-1.17% 的正向
  量级；本轮确认两者都能在 e00051 的 10 开关基座上独立迁移并保持语义正确，
  但没有新增同窗锚点，故不升级这些量级结论。
- **资源约束趋紧。** e00055/e00056 的 `compile_s` 分别为 1984.5s/1986.6s，
  只剩约 17% 编译预算。后续候选若扩大生成代码，必须同时给出静态 engagement
  和编译增量；activity-summary 还与已有 source-word guard 有潜在覆盖，重探前应
  先测实际 open/skip 动态证据。

## 当前 run 与方向裁决

- ledger 的当前 `best_overall` 为 e00056（241.956s），相对 gsim e00052 的名义
  比值为 **5.276x**；距离“AM <= gsim”目标仍很远。由于候选与 gsim/基线处于不同
  进程状态窗口，该比值只作状态看板，不作机制收益证明。
- 两条轨迹各完成 1/8 步，候选评估 6/32；无需调整 C/L/K 或改变机制大方向。继续
  保持跨轨迹独立，下一步先刷新各自池地图，再用同窗锚点裁决亚 10% 候选；本 run
  内不临时改测量协议或追加评估。
- 状态机登记后下一 action 将是 `r003/t0/s02`（round-robin 少步优先）。本笔记
  完成后停止，不在本 action 内调用 `begin-step`、evaluator 或其它下一步命令。

