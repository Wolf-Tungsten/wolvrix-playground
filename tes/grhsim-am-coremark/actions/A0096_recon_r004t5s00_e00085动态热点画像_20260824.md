# A0096 recon：r004/t5/s00 e00085 动态热点画像（2026-08-24）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t5**；
被画像 tip：AM 基线 **e00085**（commit `1563c3d837fc`，完整 12 开关生产表型）。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00085 \
  --out build/tes/grhsim-am-coremark/recon/r004-t5-s00
```

recon 以 e00085 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本全程持有
`build/tes/LOCK`，运行期间未并发任何构建或仿真负载。完整流程墙钟
**981.0s**。profile run 得到 `instrCnt/cycleCnt=73580/49996`，与 AM 金标
精确一致，在线 difftest 门通过。产物位于
`build/tes/grhsim-am-coremark/recon/r004-t5-s00/`（`report.md`、`report.json`、
`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 Host 时间为 **229.859s**；它包含逐块 `rdtsc` 和计数开销，
不是正式计时，不得与 e00085 的 193.403s 基线相减或写入 score。本 action
没有候选、新 eval-id 或性能裁决。

## 动态总账

| 项目 | 动态结果 | 解读 |
|---|---:|---|
| eval calls / rounds | 100,102 / 200,154 | **1.9995 rounds/eval**，调度轮数稳定 |
| compute | **167.652s / 73.1234%** | 596,466,270 次 compute-block 执行 |
| commit | **61.566s / 26.8525%** | 15,732,466 次 commit-block 执行 |
| other | 0.055s / 0.0240% | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | backward 是 forward 的约 1.184x |
| changed marks / clears | 200,264 / 200,264 | 成对出现，约 2.0006/eval |
| blocks / block cycles | 93,601 / 884,809,842,720 | 以下占比均以总块 cycles 为分母 |

commit block 仅占全部 block 执行的约 **2.57%**，却占阶段墙钟
**26.85%**；平均每次 commit-block 执行约为 compute-block 的 **13.92x**。
commit 是高单次成本池，但 compute 的 73.12% 总量仍是更大的整体优化面。

热点集中度为：top-2 **8.421%**、top-5 **14.322%**、top-10 **18.520%**、
top-20 **25.164%**、top-50 **39.978%**。top-50 外仍有 **60.022%** 长尾，
候选必须覆盖同构操作族，不能仅优化一个静态站点。

## 最高权重块与可操作池

| rank | block | role / 静态形态 | execs | cycles | cycles% |
|---:|---:|---|---:|---:|---:|
| 1 | 93159 | commit，8,464 atoms/instrs | 100,103 | 49,857,173,215 | **5.635%** |
| 2 | 93141 | commit，12,189 atoms/instrs | 100,102 | 24,647,972,712 | **2.786%** |
| 3 | 90657 | compute，7,003 atoms / 7,068 instrs | 100,791 | 23,893,587,565 | **2.700%** |
| 4 | 90656 | compute，7,001 atoms / 7,757 instrs | 88,260 | 17,631,567,115 | **1.993%** |
| 5 | 83835 | compute，7,003 atoms / 8,927 instrs | 50,000 | 10,691,181,963 | **1.208%** |
| 6 | 93085 | compute，6,217 atoms / 8,125 instrs | 100,053 | 9,880,682,441 | **1.117%** |

- b93159/b93141 合计 **8.421%**，是最集中的 commit 主峰。两块每次进入时
  分别先清零 `1313+870+559=2,742` 和 `3232+814+181=4,227` 个
  `arrChg/wrChg/detGrp` 布尔 scratch，然后 chunk 0 才计算事件边沿；后续
  主体 chunk 只在边沿成立时执行。这给出了可验证的“事件前固定 scratch 清零”成本。
- b90656/b90657 合计 **4.693%**，是最大的连贯 compute 双峰。生成代码中密集排列
  outlined system-task 的 fire 谓词、handle/参数准备和冷体调用；每块约 7k atoms
  且分别执行 88,260/100,791 次，支持池级共享谓词或无效参数准备消除，不支持只给
  单个分支加 hint。
- b83835/b93085 合计 **2.325%**，包含宽值搬运、比较/归约和密集标量算术。该池
  动态上限低于前两池，只有覆盖同构操作族的专化才值得占用候选席位。

## 裁决与下一步建议

本 recon 成功刷新 r004/t5 的动态证据，功能门通过且不消耗 eval-id。r004/t5/s01
的 K=2 局部采样可从两个机制互异的池构建：

1. 对 b93159/b93141 先单独计算事件边沿，只在主体 chunk 真正执行时才创建/清零
   scratch，不改写回顺序和 changed 语义。要求双峰 cycles 至少下降 15%、整体
   Host 下降至少 1%；池级降幅低于 5% 则证伪。
2. 对 b90656/b90657 中共享完全相同 fire 谓词的相邻 outlined host call 生成谓词
   run，一次求值后在 run 内调用，保持每个 task 的 handle/参数和调用次序。要求该
   双峰 cycles 至少下降 20%、整体 Host 下降至少 1%；池级降幅低于 5% 则证伪。

两项建议只使用 t5/e00085 本次 recon 和冻结起点代码的证据，不引用 r004 其他轨迹
的候选结果。top-50 外 60.022% 的长尾也要求下一步先证明池级 engagement，不以静态
站点数替代动态权重。本 action 只完成 recon，不启动后续 step。`record-recon` 登记后，
状态机下一 action 应为 **r004/t5/s01 step（K=2）**。
