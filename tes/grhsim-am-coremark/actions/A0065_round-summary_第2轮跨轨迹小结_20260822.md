# A0065 round-summary - r003 第 2 轮跨轨迹小结（2026-08-22）

对应 `next` = `round-summary`（round 2）。本 action 无评估开销；数据全部来自
`state/ledger.jsonl`（r003 e00057-e00060）以及 A0063/A0064 step 笔记。

## 本轮各轨迹 winner 与假设

| 轨迹 | step | winner | Host 中位 | vs Phi 节点（名义） | CV | 假设（一句话） | 裁决 |
|---|---|---|---:|---:|---:|---|---|
| t0 | s02 | e00057 (c1) | **229.429s** | **-7.32%** vs e00054 | 0.00% | `--scan-branch-hints` 在 outline 基座上给 byte/Block 稀疏活动扫描加冷分支提示，压紧 fall-through 取指流，预期至少降 2%，低于 0.5% 证伪 | **机械确认**；已入 `t0/main`（`1563c3d`）并成为 `best_overall` |
| t1 | s02 | e00059 (c1) | **257.235s** | **+6.32%** vs e00056 | 0.00% | `--wide-mux-chain-priority-resolve` 对 5 条多级融合链从高优先级解析 selector mask，使每个 lane 只写一次，预期至少降 2%，低于 1% 或回退证伪 | **机械 winner、机制未确认**；已入 `t1/main`（`d28a44f`），但 t1 best 仍为 e00056 |

两项落选候选同样通过全部功能门。t0 c2 e00058
`--sys-task-body-compact` = 252.374s（相对 e00054 名义 +1.94%，CV 0.00%），
共享无参数 `fwrite` 冷体没有产生运行时或编译收益。t1 c2 e00060
`--wide-mux-chain-single-level-tile` = 358.271s（相对 e00056 名义 +48.07%，
CV 0.00%），精确命中 151 条单级链；但 `e00060/e00059=1.393x` 落入已知
per-process 快慢态带，raw 分差不能隔离 tile/scatter 机制。四个候选均为
`status=ok`，17/17 ctest、3 rep difftest（73580/49996）全过，`compile_s`
为 1064.0/1072.4/2002.4/1983.2s，均在 2400s 门内。

## 跨轨迹对比

- **分数曲线分化。** t0 的机械主线为 y0 e00051 363.995s -> e00054
  247.560s -> e00057 229.429s，连续两步刷新 best；t1 为 e00051 363.995s ->
  e00056 241.956s -> e00059 257.235s，第二步机械前进但历史 best 未刷新。
  这些点跨评估窗口，只描述 ledger 轨迹，不把名义斜率当成机制净收益。
- **收敛：已有动态病灶支撑的流形态改造继续胜出。** scan hints 针对已量化的
  dispatch 跳过链，r002 已在 t0/t1 同窗确认 -11.41%/-5.69%，本轮在 outline
  基座上又给出名义 -7.32%；这组跨 run 一致性继续支持前端取指流方向。
- **发散：仅凭静态覆盖做 helper 微结构精修没有兑现。** priority-mask 命中
  5 条多级链却名义回退，single-level tile 命中 151 条链也没有正收益证据；
  `chain/level` 数量不能替代动态 block execs、selector 密度和生产二进制形态。
  与之类似，task-body compact 虽减少重复冷体，却增加共享调用/参数物化边界，
  说明代码体积继续缩小不必然改善热路径。
- **测量限制没有收敛。** e00059/e00060 批内 CV 均为 0，但两批比值正好为
  1.393x；低 CV 仍不能排除整批快/慢态。跨窗口的 e00057/e00054 -7.32% 和
  e00059/e00056 +6.32% 均只作方向辅助，亚 10% 的新机制仍需同窗或同态证据。

## 当前 run 与方向裁决

- 当前 `best_overall` 为 t0/e00057（229.429s），相对 AM 基线 e00051 名义改善
  **36.97%**，相对 gsim e00052（45.864s）的看板比为 **5.002x**；目标仍未达到，
  且跨进程状态使该比例不能作为纯机制累计收益。
- t0/t1 各完成 2/8，TES eval 计数 10/32。无需用户调整 C/L/K 或改变任务方向；
  后续维持轨迹独立，并把候选门槛收紧为“先有动态权重/二进制证据，再改 helper
  微结构”。`sys-task-body-compact` 暂关闭；priority-mask 与 single-level tile
  在补齐动态证据前不原样重测。
- 状态机登记后下一 action 将是 `r003/t0/s03`。本笔记完成后停止，不在本 action
  内调用 `begin-step`、evaluator 或其它下一步命令。
