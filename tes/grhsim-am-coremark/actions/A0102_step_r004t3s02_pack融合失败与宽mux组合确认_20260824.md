# A0102 step：r004/t3/s02 pack 融合失败与宽 mux 组合确认（2026-08-24）

对应当前 `next` = `step`，轨迹 `t3`，step 2/4，K=2。Phi 唯一来源为
r004/t3/s01 winner e00093（commit `3abdf544eb6e`，Host **172.530s**）；两个候选
都继承其 `--memory-read-pow2-index` 表型，并分别细化六个 512-read 热块的
read-to-concat 物化，以及组合本轨迹 e00094 已通过功能门的宽 mux 链融合。动态病灶
均来自最新 recon `r004-t3-s00/e00085`：六个幂次 memory-read 块合计占总块 cycles
**3.140%**，b69157/b69158/b69159 宽链池占 **1.567%**。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--memory-read-pack-fuse` | e00105 / `09b1f01` | e00093 -> 六个 512-read 热块仍占旧画像 **3.140%**，每块保留 512 个窄 scalar 到随后 1024-bit Concat -> 仅对同块连续、sole-use、精确幂次索引的 read 群直接生成逐 word packed gather -> 热池至少下降 20%、Host 较 e00093 至少下降 0.75% | **ctest_fail 16/17**，`compile_s=227.4s`；新 fixture 把 MemoryRead 放进禁止该 opcode 的 EntryBlock，语义 validator 在生产 emit 前拒绝；无 difftest/Host | 测试建模错误阻断，机制性能**未测**；失败照常登记，不把它当作负性能证据 |
| c2 `--array-broadcast-mux-chain-fuse` 组合 | e00106 / `8c4585f` | e00093 -> e00094 已在同轨迹证明 92-step 宽链融合功能正确且名义改善 3.06%，源池动态权重 **1.567%** -> 把该独立宽链 pass 与幂次索引组合 -> 保留 memory-read 收益且 Host 再降至少 0.75% | **166.947s**（166.947/164.090/167.586s，CV 1.12%，单簇、非 noisy），`compile_s=642.8s`；17/17 ctest，三次 difftest 均为 `73580/49996` | 较 e00093 **-3.24%**，越过 r004 3% 裁决带；较此前全局 best e00104 **-1.55%**，winner |

两份 `tes-candidate.json` 都相对 run 冻结的 12 项表型声明完整差集：c1 增加
`--memory-read-pow2-index --memory-read-pack-fuse`，c2 增加
`--memory-read-pow2-index --array-broadcast-mux-chain-fuse`；正式 evaluator 的
`result.json.emit_args` 与声明逐项一致。两次评估严格串行，e00105 登记结束并释放
全局锁后才启动 e00106。

## 裁决与机制分析

- `finish-step` 按唯一 ok 分数选择 e00106，将 commit `8c4585f67438` 快移到
  `tes/r004/t3/main`，相对父 e00093 记 outcome=`win`。全局 best 从 e00104
  **169.577s** 刷新为 **166.947s**；预算变为 **22/48**。冻结口径下相对 AM y0
  193.403s 改善 **13.68%**，AM/gsim = **7.348x**。
- c2 的收益方向与 e00094 一致，但这次是在 e00093 的幂次索引基座上获得干净的
  3.24% 增量，证明两个机制可组合，而不是把 e00094 的跨候选名义读数直接相加。
  生产表型仍只融合目标族 4 链/92 steps；未做 post-change recon，因此不能宣称
  b69157/b69158/b69159 池达到预注册 70% 降幅，下一次画像应验证池权重和替代热点。
- c1 的生产优化带有严格 fallback：连续性、sole-use、精确索引域或 word packing
  任一不满足即保留原路径。正式失败发生在候选自带 fixture 的 EntryBlock 合法性，
  生产输入尚未 lower/emit；所以 ledger 状态是有效的 `ctest_fail`，机制结论只能是
  未测。若未来重开，必须先把 fixture 放入普通 compute Block，并在正式 eval 前让
  该测试实际命中 pack group；不得原样重测当前 commit。

## 后续建议

t3 的后续 recon 优先基于 e00106 量化六个 memory-read 块和三个宽 mux 链块的最新
cycles 权重，确认 3.24% 组合增量由宽链池下降而不是构建布局偶然性。pack gather
只有在修正 fixture、证明生产六块实际命中且生成代码消除 3,072 个 scalar live
range 后才值得重新占用席位；静态设计本身不作为收益证据。

状态机收口后预告的下一 action 是 **r004/t4/s02 step**；本 action 未启动它。
