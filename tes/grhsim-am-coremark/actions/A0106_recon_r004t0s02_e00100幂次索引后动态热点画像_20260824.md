# A0106 recon：r004/t0/s02 e00100 幂次索引后动态热点画像（2026-08-24）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t0**；
被画像 tip：**e00100**（commit `8cb0b6ee1bfe`，Host **172.832s**，完整 14 开关
生产表型）。本次重点核验 `--memory-read-pow2-index` 对六个 512-depth memory-read
热块的动态降幅，并刷新 t0 下一 step 的病灶权重。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00100 \
  --out build/tes/grhsim-am-coremark/recon/r004-t0-s02
```

recon 以 e00100 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本全程持有
`build/tes/LOCK`，运行期间未并发任何构建或仿真负载。完整流程墙钟
**850.2s**。profile run 得到 `instrCnt/cycleCnt=73580/49996`，与 AM 金标
精确一致，在线 difftest 门通过。产物位于
`build/tes/grhsim-am-coremark/recon/r004-t0-s02/`（`report.md`、`report.json`、
`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 eval 时间为 **212.834s**；它包含逐块 `rdtsc` 和计数开销，
不是正式计时，不得与 e00100 的 172.832s score 相减或写入裁决。本 action
没有候选、新 eval-id 或性能裁决。

## 动态总账

| 项目 | e00100 动态结果 | 相对 t0/s00 e00085 同口径 | 解读 |
|---|---:|---:|---|
| eval calls / rounds | 100,102 / 200,154 | 不变 | **1.9995 rounds/eval**，调度工作量稳定 |
| compute | **151.464s / 71.1651%** | 166.733s -> 151.464s（**-9.16%**） | 本次总降幅主要落在 compute |
| commit | **61.314s / 28.8082%** | 60.234s -> 61.314s（+1.79%） | 未见 commit 池随整体下降 |
| other | 0.057s / 0.0267% | 近似不变 | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | 不变 | 优化未改变调度激活总量 |
| changed marks / clears | 200,264 / 200,264 | 不变 | changed 机械未改变 |
| blocks / block cycles | 93,601 / **817,560,683,242** | 876,687,574,636 -> 817,560,683,242（**-6.74%**） | 与正式 Host 的确认改善方向一致 |

热点集中度为：top-2 **9.076%**、top-5 **15.455%**、top-10 **20.033%**、
top-20 **27.156%**、top-50 **41.909%**。top-50 中 commit 块占总 cycles
**30.028%**，compute 块占 **11.881%**；top-50 外仍有 **58.091%** 长尾。
compute 热池被清除后，commit 在剩余总账中的相对权重明显上升。

## 幂次索引机制核验

| 动态池 | t0/s00 e00085 | t0/s02 e00100 | 变化 |
|---|---:|---:|---:|
| b9029/b9041/b9047/b9062/b9063/b9089 execs | 158,668 | 158,668 | 不变 |
| 六块 cycles | 27,064,077,517 | 1,491,801,872 | **-94.49%** |
| 六块 cycles / 全部 block cycles | **3.087%** | **0.182%** | **-2.905pp** |

六块各自原有的 512 个 `index_words(..., 9, 512)` 已由幂次索引专化消除，
动态执行次数完全不变而 cycles 几乎清空，证明收益来自每次 memory-read 的
索引机械下降，而不是调度工作量变化。e00100 正式 Host 较父 e00088 改善
**8.95%**，本 recon 又通过池级 25% 的预设门槛；该机制从“Host 相关性”升级为
动态因果闭环。六块只剩 0.182%，继续细化同一池没有足够收益上限，应视为饱和。

## 剩余最高权重池

| block / 池 | role | execs | cycles | cycles% | 相对 e00085 同块 cycles |
|---|---|---:|---:|---:|---:|
| b93159 | commit | 100,103 | 49,776,974,137 | **6.088%** | -1.19% |
| b93141 | commit | 100,102 | 24,427,929,231 | **2.988%** | -3.06% |
| b90657 | compute / system-task | 100,791 | 21,633,034,156 | **2.646%** | -2.08% |
| b90656 | compute / system-task | 88,260 | 18,817,457,793 | **2.302%** | -0.77% |
| b93085 | compute / Div 等混合池 | 100,053 | 11,701,161,381 | **1.431%** | +15.18% |
| b83835 | compute / 宽值与归约 | 50,000 | 9,894,498,578 | **1.210%** | -8.39% |

- b93159/b93141 合计 **9.076%**，绝对 cycles 只下降 1.81%，未分享 compute
  总账的降幅；它们及相邻 commit 块构成当前最集中的剩余成本池。已有 TES
  证据否决过原样 branchless 写回和行 RMW 合并，后续必须是能解释 changed/scratch
  数据机械的新机制，不能仅凭权重重测旧实现。
- b90656/b90657 合计 **4.948%**，绝对 cycles 仅下降 1.47%；此前 event-first
  只有噪声带内弱正且组合回退，继续做条件顺序、hint 或冷体布局精修缺乏依据。
- b93085 在插桩画像中未随 `--inline-unsigned-div64` 下降，反而绝对 cycles
  上升 15.18%。单次 profile 的代码布局与插桩噪声不足以把增幅归因给开关，但足以
  否定“该开关已清除整个 b93085 病灶”；该块混合成本尚未被分解，不宜继续做
  unsigned-div helper 边界微调。
- b69157/b69158/b69159 宽 broadcast-to-mux 流扫合计 **1.683%**；绝对 cycles
  相对 e00085 仅下降 0.16%，仍是未被 t0 当前表型触及的连贯 compute 池。

## 裁决与下一步建议

本 recon 成功刷新 r004/t0 的动态证据，功能门通过且预算保持 **26/48**。
`--memory-read-pow2-index` 已完成“静态 engagement -> 正式 Host 改善 -> 动态池
cycles -94.49%”闭环，同池后续优化关闭。r004/t0/s03 的 K=2 局部采样建议：

1. 以 b69157/b69158/b69159 的 **1.683%** 动态池为锚，考虑迁移已确认的宽 mux
   链融合机制；若使用跨轨迹结果，须占用本 step 唯一 migration 席位并明确来源
   eval。可证伪预期应要求该池 cycles 显著下降，并以整体 Host 越过 1% 为下限。
2. 本轨迹本地席位优先围绕 b93159/b93141 及相邻 commit 家族提出区别于已否决
   branchless/RMW 的池级机制；实施前先量化 changed/scratch 检查与有效写比例，
   只有静态覆盖和预期净收益足以解释至少 3% Host 时才进入正式候选。

top-50 外仍有 58.091% 长尾，任何候选都必须证明同构池 engagement。本 action
只完成 recon，不启动后续 step。`record-recon` 登记后，状态机下一 action 应为
**r004/t0/s03 step（K=2）**。
