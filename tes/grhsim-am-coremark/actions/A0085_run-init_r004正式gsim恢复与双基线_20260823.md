# A0085 run-init：r004 正式 gsim 恢复与串行双基线（2026-08-23）

类型：run-init（覆盖正式 gsim 前置恢复、初始化、输入指纹和全部基线测量）。
run：**r004**（C=6, L=4, K=2，搜索预算 N=48）。A0084 的前置阻塞至此解除。

## 正式 gsim emu

- 按标准生产流程从 `reference/gsim@a6a5183f7cf73cf30be00f9779063016378e751c`
  构建 `build/xs/gsim/gsim-compile/emu`；不是 `gsim-flat`，也没有用软链替代。
- 图参数为 `--supernode-max-size=15`，未带 `--flatten-nodes`；最终图含 84,643 个
  supernode、645,854 条 DAG edge。生成时 `GSIM_EMIT_RUNTIME_PROFILE=0`，生成源码中
  没有 runtime profiling 插桩。
- emu 大小 55,836,168 bytes，SHA-256 为
  `73e62bd16590a3dfb2a7d6dbe711be8843383f9fdfa28f8b20603996542a4418`。
- 迁移后的首次链接前编译因系统搜索路径缺 `zstd.h` 失败；改用仓库本地
  `build/dependency/root` 的 include/library 路径增量续编，复用了已生成的标准图，
  最终成功链接。完整日志为
  `build/logs/xs/xs_gsim_build_20260823_formal_gsim.log` 和
  `build/logs/xs/xs_gsim_build_20260823_formal_gsim_resume.log`。
- 台账外单次冒烟为 22.694s、`instrCnt/cycleCnt=73584/49998`，nemu difftest 通过。

## r004 初始化

1. 以 r003/e00057 的精确解初始化 r004：代码 commit
   `1563c3d837fcfe9db28fc36901531a70b59fd790`，冻结其完整 12 开关表型。
2. 创建 `tes/r004/base`、`tes/r004/t0/main` 至 `tes/r004/t5/main`；隔离基线
   worktree 为 `build/tes/grhsim-am-coremark/src/base-r004`。
3. 冻结迁移后 post-stats 输入 SHA-256：
   `c82ed4542f58ba60b1d7b38c57a877ad7cc81898a5df019d72d7e885588b70c7`。

## 本机串行双基线

| side | eval | core 12 / 13 / 14 | 快簇中位 | CV | 功能门 |
|---|---|---:|---:|---:|---|
| AM y0 | e00085 | 193.403 / 188.958 / 194.839s | **193.403s** | 1.59% | 17/17 ctest；三次 difftest `73580/49996` |
| gsim target | e00086 | 22.720 / 22.608 / 23.100s | **22.720s** | 1.13% | 三次 difftest `73584/49998` |

两批均按 core 12、13、14 逐次串行，均为单簇、非 noisy。AM 冷构建
`compile_s=645.3s`。本机同协议重锚后，AM/gsim = **8.512x**，即 AM 仍比目标慢
751.25%；该数值替代迁移前 r003 的 5.002x，二者不作机制收益比较。

## 裁决

r004 run-init 完成，正式 gsim target 已恢复且双基线均通过强制功能门。状态机下一
action 为 t0 recon（对 e00085 做非计时 profiling）；本 action 不执行 recon 或搜索 step。
