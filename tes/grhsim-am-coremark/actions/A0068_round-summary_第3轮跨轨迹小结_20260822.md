# A0068 round-summary - r003 第 3 轮跨轨迹小结（2026-08-22）

对应 `next` = `round-summary`（round 3）。本 action 无评估开销；数据全部来自
`state/ledger.jsonl`（r003 e00061-e00064）以及 A0066/A0067 step 笔记。

## 本轮各轨迹 winner 与假设

| 轨迹 | step | winner | Host 中位 | vs 比较节点（名义） | CV | 假设（一句话） | 裁决 |
|---|---|---|---:|---:|---:|---|---|
| t0 | s03 | e00061 (c1) | **251.746s** | **+9.73%** vs e00057 | 0.00% | `--scan-active-byte-ctz` 用 `ctz + switch` 只派发活跃 byte 中实际置位的 Block，预期至少降 2%，低于 0.75% 或回退证伪 | **机械 winner、机制证伪**；已入 `t0/main`（`3706873`），但 t0 best 仍为 e00057 |
| t1 | s03 | e00064 (c2) | **409.731s** | **+59.28%** vs e00059 | 0.00% | `--wide-mux-chain-zero-tile-bypass` 先按 tile 汇总 selector，全零 tile 连续落 base，非零 tile 才执行原 branchless replay，预期至少降 1.5%，低于 0.5% 或回退证伪 | **高负载窗机械 winner、绝对收益未确认**；较 c1 快 17.31%，已入 `t1/main`（`0f3fd04`），但 t1 best 仍为 e00056 |

两项落选候选同样通过全部功能门。t0 c2 e00062 `--sys-task-body-cold` =
300.215s（相对 e00057 名义 +30.85%，CV 0.00%）；7,235 个 cold helper 确实形成
约 6.11MB `.text.unlikely`，最终 ELF `.text` 缩小 2.66%，但没有运行时正收益。
t1 c1 e00063 `--wide-mux-chain-sparse-overlay` = 495.496s（相对 e00059 名义
+92.62%，CV 8.73% noisy）；它消除了 unresolved base lane 的逐位枚举，但在
loadavg 49.50 的慢窗没有正收益证据。四个候选均为 `status=ok`，17/17 ctest、
3 rep difftest（73580/49996）全过，`compile_s` 为
1068.4/1070.5/2031.3/2229.3s，均在 2400s 门内。

## 跨轨迹对比

- **分数曲线同时离开各自历史 best。** t0 的机械主线为 y0 e00051 363.995s ->
  e00054 247.560s -> e00057 229.429s -> e00061 251.746s；t1 为 e00051
  363.995s -> e00056 241.956s -> e00059 257.235s -> e00064 409.731s。
  t0 本步 winner 较 best 名义慢 9.73%，t1 本步 winner 较 best e00056 名义慢
  69.34%；这些跨窗口差值只描述 ledger 轨迹，不视为机制净回退。
- **收敛：稀疏性只有在保留连续数据流时才可能兑现。** t0 的 `ctz + switch` 为
  93,599 个 Block 引入全模型 jump table，使 ELF `.text` +2.48%、`.rodata`
  +7.67%，未胜过已确认的 branch-hinted 线性测试；t1 的全局 sparse overlay
  同样没有正收益，而局部 zero-tile bypass 保留连续 base 流，在相近高 loadavg
  窗口机械快 17.31%。因此保留局部 tile 门控作待量化方向，关闭全局枚举/散写的
  原样实现；该相对排名不升级为绝对因果量级。
- **收敛：静态代码体积不能替代热路径证据。** task cold 属性真实缩小最终
  `.text`，运行时仍无正收益；结合上一轮 `sys-task-body-compact` 负结果，保留
  “把 body 移出热 Block”的 outline 主机制，关闭已移出冷体的纯布局/体积精修。
- **发散：两条轨迹的证据强度不同。** t0 的 ctz 形态同时有运行负反馈与二进制
  膨胀，可按预注册门关闭；t1 的 e00063/e00064 起跑 loadavg 分别为 49.50/50.18，
  且 e00063 noisy，只能确认状态机 winner 和设计相对优先级，不能判定 zero-tile
  bypass 相对低负载历史节点的净效果。
- **测量限制进一步暴露。** e00063 三 rep 分裂为 574.430/495.496/495.495s，证明
  固定 3 rep 内也会出现状态跳变；e00064 虽 CV=0，整批仍处高负载慢态。继续遵守
  冻结的 3-rep 协议，不扩增、不补测；跨窗和 noisy 分数只作状态机输入与方向辅助。

## 当前 run 与方向裁决

- 当前 `best_overall` 仍为 t0/e00057（229.429s），相对 AM 基线 e00051 名义改善
  **36.97%**，相对 gsim e00052（45.864s）的看板比为 **5.002x**；目标仍未达到。
- t0/t1 各完成 3/8，TES eval 计数 14/32。无需用户调整 C/L/K 或改变任务方向；
  后续维持轨迹独立。t0 不原样重测 `scan-active-byte-ctz`、
  `sys-task-body-compact/cold`；t1 若再选中宽 mux 邻域，先做非计时动态计数量化
  zero/active tile 比例，并保持公共 helper 与代码体量，不原样重测 priority-resolve
  或 sparse-overlay。
- 状态机登记后下一 action 为 `r003/t0/s04`。本笔记完成后停止，不在本 action 内
  调用 `begin-step`、evaluator 或其它下一步命令；本小结的跨轨迹观察也不回流到
  当前 run 的 proposal。
