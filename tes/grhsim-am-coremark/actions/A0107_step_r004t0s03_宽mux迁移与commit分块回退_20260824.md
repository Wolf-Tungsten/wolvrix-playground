# A0107 step：r004/t0/s03 宽 mux 迁移与 commit 分块回退（2026-08-24）

对应当前 `next` = `step`，轨迹 `t0`，step 3/4，K=2。Phi 选择 e00100
（Host **172.832s**）与基线 e00085 作为 refinement 来源；最新动态证据为
`build/tes/grhsim-am-coremark/recon/r004-t0-s02/report.md`。c1 使用本 step 唯一迁移席，
把 e00106 已确认的宽 ArrayBroadcast-to-ArrayMux 链融合叠加到 e00100；c2 保持本轨迹
独立，以 commit 双峰的 chunk 调用边界为局部机制，将 3000-instruction chunk 调到 6000。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--array-broadcast-mux-chain-fuse` | e00111 / `611784b` | e00100 + 迁移源 e00106 -> post-change recon 中 b69157/b69158/b69159 仍占总块 cycles **1.683%**，e00106 已确认宽 mux 融合较幂次索引父节点改善 3.24% -> 在 e00100 的 division + pow2 表型上融合相邻 64-bit lane 链，只物化 352-lane 尾值 -> 生产命中同一 4 链/92 steps，Host 较 e00100 至少改善 1% | **170.564s**（170.781/168.473/170.564s，CV 0.75%，单簇、非 noisy），`compile_s=648.4s`；17/17 ctest 与 3 rep difftest 全过；较 e00100 改善 **1.31%** | 达到 1% 下限但未越 3% 确认带；`migration_source=e00106`，raw winner，outcome=`neutral`，合入 t0/main |
| c2 `--block-chunk-instructions 6000` | e00112 / `1306f32` | e00100 -> b93159/b93141 各执行约 100k 次、合计占 **9.076%** cycles，8464/12189 条指令在 3000 粒度下跨 4/6 chunks -> 将粒度加倍，减少热块跨函数调用 -> Host 至少改善 1% | **174.258s**（174.258/170.895/176.138s，CV 1.53%，单簇、非 noisy），`compile_s=645.3s`；17/17 ctest 与 3 rep difftest 全过；较 e00100 回退 **0.83%** | 未达 Host 门，不合入；生成物显示 gate-head 独立边界使 chunks 实际仅降为 3/4，预期调用减半未实现 |

两个候选的 `tes-candidate.json` 都声明相对冻结 12 开关的完整表型增量；正式
`result.json.emit_args` 与声明逐项一致。两次 evaluator 严格串行运行，仅由任务评估器
执行隔离构建、17 项回归、金标 difftest 与绑核 3 rep，均远低于 2400s 编译门。

## 裁决与机制分析

- `finish-step` 按 raw score 选择 e00111，并将 commit `611784b` 快移到
  `tes/r004/t0/main`。e00111 比 e00112 快 **3.694s（2.12%）**，但相对父 e00100 的
  1.31% 增量仍在 ±3% 噪声带内，因此状态机记 outcome=`neutral`；t0 的统计 best
  仍为 e00100 **172.832s**，tip 则更新为 e00111。
- c1 的生产 emit 明确报告 `array_broadcast_mux_chains=4`、`steps=92`，说明迁移表型
  已激活且覆盖与 e00106 相同。整体 Host 只给出弱正复现，不能用 1.31% 反推 1.683%
  热池的动态降幅；若后续需要确认可加性，应做 post-change recon，而不是再次迁移重测。
- c2 的预注册假设高估了调用削减：commit gate 的头 detector 必须保持独立 chunk，故
  b93159 只从 **4 降到 3** 个 chunk，b93141 从 **6 降到 4**，而非减半。即使减少
  3 个热调用，Host 仍回退 0.83%，表明 commit 主峰成本主要在写站/changed/scratch
  数据机械，不在当前 chunk 函数边界；6000 粒度轴在此表型下关闭。
- e00111 相对冻结 AM/e00085 改善 **11.81%**，冻结 gsim/e00086 口径为 **7.507x**；
  全局 best 仍为 t3/e00106 **166.947s**（7.348x）。预算从 26/48 增至 **28/48**。

## 后续建议

下一 action 由状态机给出 **t1/e00102 recon**，应先刷新幂次索引后的动态热点，再设计
t1/s03；不要把 t0 的弱正 mux 迁移当作确认增量，也不要继续扫 commit chunk 粒度。
若 t0 后续再获 recon 席位，优先核验 mux 池是否降权以及 commit 双峰内部的 changed/
scratch 数据成本。本 action 到此停止，未启动下一 action。
