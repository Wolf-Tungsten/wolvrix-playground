# A0124 step：r004/t5/s04 commit source 分片与 lazy scratch 开关失败（2026-08-25）

对应 `next` = `step-resume`，轨迹 `t5`，step 4/4，K=2。Phi 选择
e00121（t5 当前主线，Host 168.198s）与 e00097；最新动态证据显示
b93159/b93141 commit 池仍是主要剩余热点。本 step 在 e00121 基座上分别检验
commit source 分片和 lazy scratch 清零的可加性。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--max-commit-source-bytes 262144` | e00133 / `d9143d1` | e00110/e00121 的 commit 双峰仍占主要成本 -> 将生成 commit source 从默认 512KiB 分片到 256KiB -> Host 至少改善 0.75% | **167.917s**（167917/166753/169881ms，CV 0.94%，unimodal non-noisy，compile_s 646.7s）；17/17 ctest、三次 73580/49996 difftest 全过 | 较父 e00121 改善 **0.17%**，低于预期和 3% 确认带；`neutral`，合入 t5/main |
| c2 `--commit-scratch-lazy-init` | e00134 / `70245c3` | e00097 的 lazy scratch 弱正 -> 与 e00121 的宽 mux、幂次索引和 scratch-after-gate 组合 -> Host 至少改善 1% | ctest 17/17 全过，但 emit 阶段返回 `invalid argument: --commit-scratch-lazy-init`；未生成生产代码、未进入 difftest/Host 计时 | `emit_fail`，不可作性能裁决 |

两个候选都声明了 e00121 的完整 15 项基座表型；c1 追加 source 分片旋钮，c2
追加 lazy scratch 旋钮。评估严格串行。c1 的三次 difftest 均达到
`instrCnt=73580`、`cycleCnt=49996`；c2 在 evaluator 的 ctest 门之后被 CLI
参数校验拒绝。

## 裁决与机制分析

- `finish-step` 选择 e00133 作为 raw winner，并将 `d9143d1` 快移到
  `tes/r004/t5/main`。它相对父 e00121 只快 281ms（0.17%），说明缩短 source
  的编译/指令缓存边界在该基座没有击穿 commit 双峰；不能升级为确认机制。t5 的
  统计 best 仍为 e00110（173.058s），全局 raw best 仍为 e00125（164.729s）。
- e00134 暴露的是实现/表型可用性问题，而非性能负结果：`--commit-scratch-lazy-init`
  在 t5 当前主线没有对应 CLI 选项。虽然 ctest 全绿，emit 未开始，不能把该候选
  视为机制证伪；若未来重开，必须先把开关实现迁移到该基座并补充 emit-cost fixture。
- 本步两项均未形成新的确认收益。commit 池仍应在 run-summary 中按清零、scan、call
  和 DPI 成本拆分后再决定是否扩大改动面。

## 对下一步的建议

t5 已完成 4/4；状态机下一 action 是 `run-summary`。总结时保留 e00133 的 source
分片为弱正/neutral 证据，记录 e00134 的 CLI 缺失，并以全局 best e00125 为
restart 讨论基准；本 action 未启动 run-summary。
