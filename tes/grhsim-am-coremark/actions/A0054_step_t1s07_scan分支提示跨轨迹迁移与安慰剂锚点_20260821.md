# A0054 step t1/s07：scan-branch-hints 跨轨迹迁移同窗确认 -5.69%（2026-08-21）

- run: r002  trajectory: t1  step: 7/8  K=2  evals: e00029/e00030（用后 30/32）
- 前置：A0052/A0053 裁定 t1/s07 首选 = t0 scan-branch-hints（A0053，同窗 -11.41%）
  跨轨迹迁移；t1 侧无名池 = recon-t1s05 dispatch 骨架缺口 ~104s/26%（未做归因
  recon，跳过链份额未知），外推 5-7%。

## 候选对比

| 候选 | eval | commit | 假设（一句话） | Host 中位 | CV | compile_s | 裁决 |
|---|---|---|---|---|---|---|---|
| c1 scan-branch-hints 迁移 | e00029 | `74b6d1e` | t0 -11.41% 机制移植 t1 链，按 t0 捕获率 ~50% 外推同窗降 ≥4%；<2% 证伪 | **322.762s** | 0.0% | 690.4s | **winner** |
| c2 同窗安慰剂锚点 | e00030 | `99c426f` | t1 tip `4471846` 原样重测（CLI 默认调度点 + 5 旋钮 + wide-mux-chain-fuse），锚定本窗 | 342.230s | 0.03% | 693.5s | 锚点 |

- 同窗差：**-19.468s = -5.69%**，越 4% 假设门，落外推区间 5-7% 内。
- 门：c1/c2 均 17/17 ctest、3 rep difftest 全过（73580/49996，金标窗内）。
- c1 emit_args = e00025 六旋钮 + `--scan-branch-hints`；c2 = e00025 同六旋钮。
- finish-step：winner e00029 已合入 `tes/r002/t1/main`（tip = `74b6d1e`）。

## 实施与静态实证

- cherry-pick `b9a671a` → 2 处冲突（emitter Options 块、lower_json_smoke usage
  串），手工解决：剔除 t0 独有 `sysTaskOutline` 伴生代码（struct +
  `sysTaskOutlinedBodies` map），仅携带 scanBranchHints；解后 diff 与原 patch
  同形（4 文件 +261/-19，与原件逐数一致）。
- 静态：生成代码 `__builtin_expect` 提示 **105,485** 处（t0 侧 105,479，逐数
  互证扫描链同构）；runtime.cpp 零提示（与 t0 相同——提示全在逐块扫描函数内）。

## 机制分析

- **为什么赢**：与 t0 A0053 同因——非激活块跳过构成「每块位测试 + ~945B 块体
  交错」的大步长取指流；`__builtin_expect(...,0)` 让 clang 把冷块体移出
  fall-through 检查链，跳过链紧凑顺序化。纯前端流式改造，不省指令。
- **量级解读**：-5.69% = 19.5s；t1 骨架无名池 ~104s 中跳过链份额未归因，若按
  t0 比例（跳过链 ≈ 缺口 80%）估 ≈83s，本步捕获 ~23%（t0 捕获 ~50%）。差异
  嫌疑：t1 携带 activity-summary-scan（摘要扫描已压过一轮跳过成本，残余池
  更薄）+ 块形状差异（15 atoms/block 下 t1 块数/字节布局与 t0 12 旋钮链不同）。
- **判据链再确认**：「数据侧/前端流式主导、省指令无效」正向逆用第二次成立
  （t0 -11.41% × t1 -5.69%），跨轨迹迁移二连中（宽链融合后第二族）。
- **dispatch 骨架轴状态**：两轨迹跳过链亚池均已兑现 ~半；残余 = 活跃 byte
  冷区位测试 + 块体本体 + 激活簿记 + 调派胶。骨架轴接近关闭，s08 开放方向
  回到 compute 相本体（长尾 52.4%）与 commit 相数据侧（约束：省指令类已三
  次证伪，只剩布局/预取且受 194MB 流扫带宽约束）。

## 测量学

- 同代码跨窗漂移样本 +1（第七点）：t1 tip `4471846` 原样 s06 窗 364.464s →
  s07 窗 342.230s（**-6.1% 快向**）——单窗内 c1 vs 锚点仍是唯一裁决基准；
  本窗为 t1 历史最快窗。
- t1 真值口径：本窗锚点 **342.230s**（e00030）、含 hints tip 本窗
  **322.762s**（e00029，t1 全部历史最低读数）。
- t1 有效 emit_args = CLI 默认调度点 + 5 旋钮 + `--wide-mux-chain-fuse` +
  `--scan-branch-hints`。

## 对 Φ 下一步的建议

- evals 30/32（余 2 = 恰好一条轨迹的完整 s08：机制 + 锚点）。t0/t1 步数
  7/8 齐平，下一 action = 第 7 轮 round-summary，由它按 recon/池证据裁定
  s08 归属：
  - t0 tip 本窗 301.081s（更低），开放池 = compute 长尾 52.8% + commit 相
    数据侧；
  - t1 tip 本窗 322.762s，开放池同向且 compute 长尾 52.4% 更厚，但 t1 无
    outline 后新 recon 池地图（recon-t1s05 基于 CLI 默认调度点，仍可用）。
  - 若 s08 给 t1：候选方向 = compute 长尾归因（需先 recon，可能无评估余量
  支撑插桩单跑——recon 不占 eval 预算但占墙钟）；若给 t0：commit 相数据侧
  布局类是唯一未触大类。
- scan-branch-hints 已入两轨迹主线，后续候选 emit_args 须显式携带全链。
