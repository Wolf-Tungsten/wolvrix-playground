# A0001 run-init：r001 初始化 + 双基线测量（含两轮系统校准）

日期：2026-08-14 ｜ run：r001（C=3 / L=8 / K=2，N=48）｜ base：`a88e7a2`

## 做了什么

1. `init-run`：manifest 冻结配置；wolvrix 建 `tes/r001/base` + t0/t1/t2 三条轨迹分支；
   pin gsim `a6a5183`、xiangshan `8f62679`；exec.json sha256 `c0210246…` 回填。
2. base worktree 建在 `build/tes/grhsim-am-coremark/src/base-r001`（子模块全部
   `--reference` 本地引用，无网络 clone）。
3. 双基线测量：第一轮双双 difftest_fail → 定位出两个系统 bug → 修复后重测通过。

## 量化结果

| 侧 | eval | status | 3-rep 中位 | reps (ms) | CV | compile_s |
|---|---|---|---|---|---|---|
| am（y0） | e00001 | ok | **273.1s** | 274290/272178/273103 | 0.39% | 1061.8s（ccache 热） |
| gsim（target） | e00002 | ok | **24.7s** | 25315/24619/24688 | 1.5% | —（现存 emu） |

**差距 11.06x。** 起点判断：这比 emit-cost 系列（NO0001–NO0018）收口时的纸面估计
（匹配对加权 ~4x）大得多，说明 AM 相对 gsim 的运行时差距里还有未被归因链覆盖的
成分（或 NO0018 的 324.0s→273.1s 差也提示机器状态/布局对单点数字的影响可达 ~15%）。
Φ 的第一步应优先做「差距构成归因」类候选而非盲调旋钮。

## 校准过程记录（两轮失败 → 修复）

- 轮 1：e00001 rep1 rc=127（`taskset: 执行 emu 失败`）；e00002 计数 73584/49998 ≠ 旧金标。
- 修复 1（evaluator bug）：`run_reps` 对相对名 `emu` 直接 exec，execvp 不搜 cwd → 一律
  解析为绝对路径（`evaluator.py` run_reps 内）。
- 修复 2（金标语义）：两种 emu 在 50k 周期窗停止点有确定性小差（gsim 73584/49998，
  am 73580/49996，均 nemu 在线核对零 mismatch）→ 功能门改为计数窗
  （73584/49998 ±16/±8，config `eval.golden_tol`），三处（config/manifest/run.json）
  与 protocol.md 同步。
- 轮 2：gsim ok（24.7s）；AM 侧 rep1 274.3s 且 difftest 干净，证明 AM 侧功能正常，
  仅是金标精确匹配不适用。
- 轮 3：AM ok（273.1s）。旧失败记录保留在两个 eval 目录的日志里可溯。

## 机制分析 / 对下一步的建议

- 评估时延构成（热 ccache）：emu_build 842s + reps ~14min 占绝对大头；emit 全量重写
  .cpp 使 emu 构建无法增量。若后续要压 eval 时延，emit「内容不变不重写」是最大杠杆
  （可省 ~14min/eval）。当前先不动，保协议稳定。
- 冷启动参考：冷 ccache 首轮 compile_s 1299s（90min 放宽预算内），之后一律 40min 预算。
- wolvrix 侧构建在 ccache 热时仅 0.4s，候选间代码小改也不会成为瓶颈。
- Φ 建议方向：先归因（profile 级对比 gsim/AM 的周期账），再定向出招；11x 的量级
  不太可能靠 emit 旋钮收敛，需要找出 AM 运行时的大头（候选：跨 chunk store→load 往返、
  状态 gather、适配胶 instr/atom 2.87x——见 insights 起点知识）。

## 下一个 action 预告

`step`（t0/s01：begin-step 生成 proposal，K=2 个机制互异候选）。
