# A0121 step：r004/t2/s04 state-write 与 host-call 操作数局部化回退（2026-08-24）

对应当前 `next` = `step`，轨迹 `t2`，step 4/4，K=2。Phi 选择 e00115（Host
**166.561s**）与 e00092；最新动态证据来自
`build/tes/grhsim-am-coremark/recon/r004-t2-s02/report.md`（基于 e00104）：
b93159/b93141 commit 双峰占总 block cycles **9.261%**，b90656/b90657
system-task 双峰占 **4.749%**。两个候选都从 t2 当前三机制 tip 出发，不使用迁移
席位，分别收紧 state-write 与非 final host-call 的 block-local 逃逸边界。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--state-write-operand-locality` | e00127 / `1611b3e` | e00115 -> b93159/b93141 占 **9.261%** -> state write 只强制真实 target 持久化，同块 data/condition/mask/address 临时量交回现有局部化分析 -> Host 至少改善 1% | **168.106s**（168.106/167.588/168.871s，CV 0.38%，单簇、非 noisy），`compile_s=568.0s`；17/17 ctest、三次 `73580/49996` difftest 全过 | 较父回退 **0.93%**，未达预期，不合入 |
| c2 `--host-call-operand-locality` | e00128 / `03c0098` | e00092 在 e00115 上继续 refinement -> predicate-run 后 b90656/b90657 仍占 **4.749%** -> 非 final SystemTask/DPI 的同块窄参数与结果允许局部化，final/跨块/早读保持持久 -> Host 至少改善 1% | **167.795s**（167.795/165.403/168.765s，CV 1.03%，单簇、非 noisy），`compile_s=638.3s`；17/17 ctest、三次 `73580/49996` difftest 全过 | 较父回退 **0.74%**；仅以 raw 分数胜 c1，outcome=`neutral` 入 t2/main |

两个正式结果均以冻结 12 项表型加父节点的
`--host-call-predicate-run --memory-read-pow2-index --array-broadcast-mux-chain-fuse`，
再分别增加自己的候选开关；最终 `tes-candidate.json` 与 `result.json.emit_args` 通过
硬审计。评估严格串行，e00127 登记并释放全局锁后才启动 e00128。

## 表型审计恢复

- c1 首次正式流水线得到 **167.464s**，但候选声明只列本 step 新开关，漏列父节点
  相对冻结表型新增的 predicate-run、pow2-index、wide-mux 三项；`record-eval`
  正确拒绝登记。随后两个候选各追加声明修复提交，c1 以同一 e00127 和完全相同的
  16 项实际表型重跑，最终只有 **168.106s** 进入 ledger。首次结果保留在 eval
  产物历史中，不作为候选分数或额外预算。
- c2 在启动前已经带修正后的四项增量声明，一次通过表型审计。该恢复没有修改
  proposal、manifest 或 run.json，eval 预算仍只增加两个席位。

## 裁决与机制分析

- `finish-step` 按 raw score 选择 e00128，并将 `03c0098` 快移到
  `tes/r004/t2/main`。它只比 e00127 快 **311ms（0.19%）**，且比父 e00115 慢
  **1.234s（0.74%）**，因此 outcome=`neutral`；这不是机制胜利。t2 的统计确认
  best 仍为 e00104 **169.577s**，全局 raw best 仍为 e00125 **164.729s**。
- c1 证明“把 commit next 锥从模型成员搬到局部标量”没有击穿 9.261% 双峰。
  现有 O3 很可能已消除多数成员往返，或更大的 chunk-local live range / 寄存器压力
  抵消了存储收益。commit 池后续不再做单纯逃逸/清零/容器形态调整；若重开，应先把
  清零、逐 flag 访问和尾部 fanout 做动态分量拆账，再让生产者与消费者按 word 批量工作。
- c2 表明 predicate-run + sys-task outlining 基座上，参数持久槽不是 4.749%
  system-task 双峰的一阶成本。立即消费语义通过功能门，但局部化更多窄值未带来 Host
  收益，可能把跨调用存活量转成了局部 live-range 压力。后续不再细化 host-call
  fire/event 或参数存储形态；需要先动态拆分 scan、参数准备、格式化与 DPI 调用成本。
- e00128 相对冻结 AM/e00085 改善 **13.24%**，冻结 gsim/e00086 口径为
  **7.385x**，目标差距仍大。预算由 42/48 增至 **44/48**。

## 对下一步的建议

t2 已完成 4/4。剩余轨迹若继续处理 commit/host-call 热池，应采用动态分量拆账驱动的
批量机制，不再重复 scratch 清零位置、位容器、逃逸边界或共享 guard/event 微调。
状态机下一 action 为 **r004/t3/s04 step（K=2）**；本 action 只作预告，未启动它。
