# A0099 step：r004/t0/s02 event 组合与幂次索引迁移（2026-08-24）

对应当前 `next` = `step`，轨迹 `t0`，step 2/4，K=2。Phi 选择本轨迹
e00088（Host **189.829s**）作为 refinement 来源；最新 t0 recon 为
`build/tes/grhsim-am-coremark/recon/r004-t0-s00/report.md`。c1 组合本轨迹 s01
的两个局部机制，c2 使用本 step 唯一迁移席位引入 t3/e00093 已确认的幂次索引机制。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--inline-unsigned-div64 --sys-task-event-first` | e00099 / `4516a5d` | e00088 + 本轨迹未中选 e00087 -> t0 recon 中 b90656/b90657 每轮约执行 88K/101K 次、合计占总块 cycles **4.683%**，event-first 单独只有弱信号 -> 在 division tip 上叠加 event/pending-first 短路 -> 较 e00088 Host 至少改善 1%，且 pending/once 状态不变 | **194.205s**（195.750/192.201/194.205s，CV 0.92%，单簇、非 noisy），`compile_s=642.0s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00088 **+2.30%** | 干净回退；组合假设否决，不合入主线 |
| c2 `--inline-unsigned-div64 --memory-read-pow2-index` | e00100 / `8cb0b6e` | e00088 + 迁移来源 e00093 -> r004/t3/s00 recon 中六个 512-depth memory-read 块合计占 cycles **3.140%**，e00093 已确认移除 3,072 个 `index_words` 站点 -> 将同一专化叠加到 division tip -> 较 e00088 Host 至少改善 5% | **172.832s**（172.832/169.400/174.662s，CV 1.55%，单簇、非 noisy），`compile_s=645.1s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00088 **-8.95%**，较 e00093 **+0.18%** | raw-score winner，outcome=`win`；以 `migration_source=e00093` 登记并合入 t0/main |

两个候选的 `tes-candidate.json` 都按 r004 冻结 12 开关表型声明两个新增开关；正式
`result.json.emit_args` 与声明逐项一致。两次 evaluator 严格串行运行，只使用任务评估器
完成全量构建、17 项回归、金标 difftest 和绑核 3 rep；均远低于 2400s 编译门。

## 裁决与机制分析

- `finish-step` 按 raw score 选择 e00100，并将 commit `8cb0b6e` 快移到
  `tes/r004/t0/main`。e00100 相对父 e00088 改善 **8.95%**，越过 3% 噪声带，记为
  `win`；t0 tip 从 189.829s 降到 **172.832s**。全局 best 仍是 t3/e00093
  **172.530s**，冻结 gsim 口径下 e00100/gsim = **7.607x**。
- c1 将同一轨迹两个各自只有噪声带内弱信号的机制组合后反而回退 2.30%。三次 rep
  单簇、CV 0.92%，没有双态或 noisy 可解释该方向反转；event-first 与 division tip
  不具可加性，不能再以“弱正相加”作为候选依据。
- c2 完整保留 e00093 的一阶收益：相对 e00088 改善 8.95%，但与 e00093 只差
  302ms（0.18%，远低于 3% 分辨带）。因此本 step 确认的是幂次索引机制可跨轨迹迁移，
  不是 unsigned-64 division 在其上仍有可分辨增量。
- c1 与 c2 分别作用于 system-task fire 谓词顺序和 memory-read 索引生成，机制互异；
  c2 是 step>=2 唯一迁移席，信息通道已通过 ledger 的 `migration_source` 明确审计。

## 后续建议

t0 下次证据到期时先对 e00100 做 post-change recon，核对六个 memory-read 块降权和
b93085 的替代热点；若 b93085 division 池没有独立动态降幅，不再继续其微结构精修。
同轨迹的 event-first + division 组合已由干净负结果关闭，后续不要把多个噪声带内弱信号
直接相加。状态机给出的下一 action 是 **r004/t1/s02 step**；本 action 只完成 t0/s02，
未启动下一 action。
