# A0100 step：r004/t1/s02 guard 跨 gap 与幂次索引迁移（2026-08-24）

对应当前 `next` = `step`，轨迹 `t1`，step 2/4，K=2。Phi 选择本轨迹
e00089（Host **190.827s**）作为 refinement 来源；最新 t1 recon 为
`build/tes/grhsim-am-coremark/recon/r004-t1-s00/report.md`。c1 局部扩大 e00089
的 host-call guard 复用范围，c2 使用本 step 唯一迁移席位引入 t3/e00093 已确认的
幂次 memory-read 索引机制。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--host-call-guard-cache` 跨安全 gap | e00101 / `c63603e` | e00089 -> t1 recon 中 b90656/b90657 每轮约执行 88K/101K 次、合计占总块 cycles **4.629%**，相邻 guard cache 仅带来 1.33% 弱正 -> 允许同签名调用跨越不写 guard/event 的普通 AM 指令共享局部 bool，不同 host call 或写 guard 指令保持屏障 -> 较 e00089 Host 至少改善 0.75% | **189.609s**（189.609/187.451/191.400s，CV 1.04%，单簇、非 noisy），`compile_s=576.2s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00089 **-0.64%** | 未达到 0.75% 预期；增量机制弱证伪，不合入主线 |
| c2 `--host-call-guard-cache --memory-read-pow2-index` | e00102 / `da88c2a` | e00089 + 迁移来源 e00093 -> r004/t3/s00 recon 中六个 512-depth memory-read 块合计占 cycles **3.140%**，e00093 已确认移除 3,072 个 `index_words` 站点 -> 将同一专化叠加到 guard-cache tip -> 较 e00089 Host 至少改善 5% | **171.233s**（171.233/169.923/172.292s，CV 0.69%，单簇、非 noisy），`compile_s=637.1s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00089 **-10.27%**，较 e00093 **-0.75%** | raw-score winner，outcome=`win`；以 `migration_source=e00093` 登记并合入 t1/main |

两个候选的 `tes-candidate.json` 都相对 r004 冻结 12 开关表型准确声明新增开关；正式
`result.json.emit_args` 与声明逐项一致。两次正式 evaluator 严格串行运行，只使用任务
评估器完成全量构建、17 项回归、金标 difftest 和绑核 3 rep，均远低于 2400s 编译门。

c1 首次 evaluator 在 wolvrix build 阶段暴露候选测试夹具枚举拼写错误
（`Opcode::BitwiseXor` 应为 `Opcode::Xor`），当时尚未进入 ctest/emit/计时，也未登记；
修复以 commit `c63603e` 追加后，同一分配 eval-id 从完整流水线重新执行并以上表终态登记。

## 裁决与机制分析

- `finish-step` 按 raw score 选择 e00102，并将 commit `da88c2a` 快移到
  `tes/r004/t1/main`。e00102 相对父 e00089 改善 **10.27%**，越过 3% 噪声带，记为
  `win`；它也以 **171.233s** 刷新全局 best，较冻结 AM 基线 e00085 改善 **11.46%**，
  冻结 gsim/e00086 口径仍为 **7.537x**。
- c1 的运行时间只比 e00089 少 1.218s（0.64%），小于预注册 0.75% 门和 3% 裁决带。
  三次 rep 单簇、CV 1.04%，没有双态或 noisy 可解释；测试证明跨安全 gap 规则生效，
  但尚无 post-change recon 或生产命中计数证明热点覆盖显著扩大。因此只能判相邻 cache
  之外的增量没有确认，不能把 0.64% 当成独立机制收益。
- c2 完整复现并略超 e00093 的一阶收益：相对 e00089 改善 10.27%，相对迁移来源
  e00093 快 1.297s（0.75%）。后者仍低于 3% 分辨带，只能说幂次索引稳定迁移且 guard
  cache 与之没有可见冲突，不能确认 guard cache 提供了可分辨的正交增量。
- c1 与 c2 分别作用于 host-call fire 谓词复用边界和 memory-read 索引生成，机制互异；
  c2 是 step>=2 唯一迁移席，信息通道已通过 ledger 的 `migration_source` 明确审计。

## 后续建议

t1 下次证据到期时先对 e00102 做 post-change recon，核对六个 memory-read 块是否按预期
降权，并检查 b90656/b90657 的剩余动态权重；在没有生产命中计数或热点降幅前，不再继续
扩张 guard-cache 边界。幂次索引已在 t0/t1 两次迁移中稳定复现，应优先寻找其专化后暴露的
替代热点，而非继续重复迁移。状态机给出的下一 action 是 **r004/t2/s02 step**；本 action
只完成 t1/s02，未启动下一 action。
