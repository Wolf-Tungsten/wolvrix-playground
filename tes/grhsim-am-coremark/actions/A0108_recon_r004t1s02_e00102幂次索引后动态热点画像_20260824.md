# A0108 recon：r004/t1/s02 e00102 幂次索引后动态热点画像（2026-08-24）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t1**；
被画像 tip：**e00102**（commit `da88c2a4c45a`，Host **171.233s**，完整 14 开关
生产表型）。本次重点核验 `--memory-read-pow2-index` 的动态降幅，并检查
`--host-call-guard-cache` 之后 system-task 热池是否获得可分辨下降。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00102 \
  --out build/tes/grhsim-am-coremark/recon/r004-t1-s02
```

recon 以 e00102 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本全程持有
`build/tes/LOCK`，运行期间未并发任何构建或仿真负载。完整流程墙钟
**864.3s**。profile run 得到 `instrCnt/cycleCnt=73580/49996`，与 AM 金标
精确一致，在线 difftest 门通过。产物位于
`build/tes/grhsim-am-coremark/recon/r004-t1-s02/`（`report.md`、`report.json`、
`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 Host 时间为 **211.883s**；它包含逐块 `rdtsc` 和计数开销，
不是正式计时，不得与 e00102 的 171.233s score 相减或写入裁决。本 action 没有
候选、新 eval-id 或性能裁决。

## 动态总账

| 项目 | e00102 动态结果 | 相对 t1/s00 e00085 同口径 | 解读 |
|---|---:|---:|---|
| eval calls / rounds | 100,102 / 200,154 | 不变 | **1.9995 rounds/eval**，调度工作量稳定 |
| compute | **150.897s / 71.4177%** | 164.898s -> 150.897s（**-8.49%**） | 总降幅来自 compute |
| commit | **60.344s / 28.5600%** | 60.328s -> 60.344s（+0.03%） | commit 绝对成本没有下降 |
| other | 0.047s | 近似不变 | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | 不变 | 优化未改变调度激活总量 |
| changed marks / clears | 200,264 / 200,264 | 不变 | changed 机械未改变 |
| blocks / block cycles | 93,601 / **811,459,620,897** | 871,204,389,767 -> 811,459,620,897（**-6.86%**） | 与正式 Host 的确认改善方向一致 |

热点集中度为：top-2 **9.194%**、top-5 **15.221%**、top-10 **19.741%**、
top-20 **26.773%**、top-50 **41.554%**。top-50 中 36 个 commit 块占总 cycles
**29.805%**，14 个 compute 块占 **11.748%**；top-50 外仍有 **58.446%** 长尾。
compute 热池被清除后，commit 在剩余总账中的相对权重继续上升。

## 机制核验与剩余热点

| 动态池 | t1/s00 e00085 | t1/s02 e00102 | 变化 |
|---|---:|---:|---:|
| b9029/b9041/b9047/b9062/b9063/b9089 execs | 158,668 | 158,668 | 不变 |
| 六个 memory-read 块 cycles | 25,786,769,719（2.960%） | 1,785,275,270（0.220%） | **-93.08%** |
| b90656/b90657 system-task cycles | 40,330,343,893（4.629%） | 38,371,722,458（4.729%） | **-4.86%** |
| b93159/b93141 commit cycles | 74,008,371,729（8.495%） | 74,606,487,624（9.194%） | **+0.81%** |
| b69157/b69158/b69159 宽 mux cycles | 13,728,298,533（1.576%） | 13,770,136,520（1.697%） | **+0.30%** |
| b83835/b93085 混合 compute cycles | 20,510,830,836（2.354%） | 20,465,294,500（2.522%） | **-0.22%** |

六个幂次深度块的执行次数完全不变而 cycles 清空 93.08%，证明 e00102 的一阶
收益来自 memory-read 索引机械下降，不是调度工作量变化。该池只剩 0.220%，
`--memory-read-pow2-index` 在 t1 也完成“正式 Host 改善 + 动态池降权”闭环，继续
细化同池没有足够收益上限。

b90656/b90657 的绝对 cycles 只下降 4.86%，其相对权重反升至 4.729%。e00102
相对迁移源 e00093 的正式 Host 优势仅 0.75%、低于 3% 分辨带；本次池级证据也不
支持把它归因于 guard-cache。跨安全 gap 的边界扩张已在 e00101 未达到 0.75% 预期，
后续关闭同类 guard-cache 范围微调，除非先有新的生产命中计数证明覆盖跃迁。

b93159/b93141 绝对 cycles 基本未动，已成为 **9.194%** 主峰；但原样 branchless
写回与行 RMW 合并已有 TES 否决，下一候选必须解释 changed/scratch 数据机械或采用
新的批量提交形态。b69157/b69158/b69159 的宽 broadcast-to-mux 流扫保持
**1.697%**，是当前最清晰、尚未被 t1 表型触及的连贯 compute 池。

## 裁决与下一步建议

本 recon 成功刷新 r004/t1 的动态证据，功能门通过，预算保持 **28/48**。建议
r004/t1/s03 的 K=2 局部采样：

1. 以 b69157/b69158/b69159 的 **1.697%** 动态池为锚，若迁移 e00106 已确认的
   宽 mux 链融合，须占用本 step 唯一 migration 席并登记来源；预期要求该池明显
   降权，整体 Host 至少改善 1%。
2. 本轨迹本地席位优先面向 b93159/b93141 的 **9.194%** commit 池提出区别于已
   否决 branchless/RMW 的批量机制；若无法给出覆盖和净收益证据，则改为对
   b90656/b90657 提出区别于 guard-cache 边界扩张的池级谓词/调用准备机制。

top-50 外仍有 58.446% 长尾，任何候选都必须证明同构池 engagement。本 action
只完成 recon，不启动后续 step。`record-recon` 登记后，状态机下一 action 应为
**r004/t1/s03 step（K=2）**。
