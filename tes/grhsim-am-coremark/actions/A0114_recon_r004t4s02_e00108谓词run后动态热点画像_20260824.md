# A0114 recon：r004/t4/s02 e00108 谓词 run 后动态热点画像（2026-08-24）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t4**；
被画像 tip：**e00108**（commit `d3d418133d1c`，Host **189.263s**，完整 13 开关
生产表型）。本次重点核验 `--sys-task-predicate-run` 是否达到预注册的
b90656/b90657 cycles 至少下降 20%，并据此刷新 t4/s03 的动态病灶权重。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00108 \
  --out build/tes/grhsim-am-coremark/recon/r004-t4-s02
```

recon 以 e00108 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本全程持有
`build/tes/LOCK`，运行期间未并发任何构建或仿真负载。完整流程墙钟
**1027.3s**。profile run 得到 `instrCnt/cycleCnt=73580/49996`，与 AM 金标
精确一致，在线 difftest 门通过。产物位于
`build/tes/grhsim-am-coremark/recon/r004-t4-s02/`（`report.md`、`report.json`、
`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 Host 时间为 **229.653s**；它包含逐块 `rdtsc` 和计数开销，
不是正式计时，不得与 e00108 的 189.263s score 相减或写入裁决。本 action 没有
候选、新 eval-id 或性能裁决。

## 动态总账

| 项目 | e00108 动态结果 | 相对 t4/s00 e00085 同口径 | 解读 |
|---|---:|---:|---|
| eval calls / rounds | 100,102 / 200,154 | 不变 | **1.9995 rounds/eval**，调度工作量稳定 |
| compute | **167.709s / 73.2073%** | 167.325s -> 167.709s（**+0.23%**） | 插桩总 compute 未呈整体降幅 |
| commit | **61.326s / 26.7695%** | 61.448s -> 61.326s（**-0.20%**） | 近似持平 |
| other | 0.053s | 近似不变 | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | 不变 | 谓词 run 未改变调度激活总量 |
| changed marks / clears | 200,264 / 200,264 | 不变 | changed 机械未改变 |
| blocks / block cycles | 93,601 / **885,080,796,049** | 883,785,828,536 -> 885,080,796,049（**+0.15%**） | 总量近似持平，正式 Host 弱正没有形成全局动态降幅 |

热点集中度为：top-2 **8.569%**、top-5 **14.357%**、top-10 **18.579%**、
top-20 **25.122%**、top-50 **39.839%**。top-50 中 commit 块占总 cycles
**27.048%**，compute 块占 **12.791%**；top-50 外仍有 **60.161%** 长尾。

## 机制核验与剩余热点

| 动态池 | t4/s00 e00085 | t4/s02 e00108 | 变化 |
|---|---:|---:|---:|
| b90656/b90657 execs | 189,051 | 189,051 | 不变 |
| b90656/b90657 system-task cycles | 43,505,361,441（4.923%） | 41,146,249,328（4.649%） | **-5.42%** |
| b93159/b93141 commit cycles | 76,416,803,863（8.647%） | 75,846,657,588（8.569%） | **-0.75%** |
| b83835/b93085 混合 compute cycles | 20,736,610,452（2.346%） | 19,818,984,805（2.239%） | **-4.43%** |
| 六个 memory-read 块 cycles | 23,454,992,450（2.654%） | 27,447,279,772（3.101%） | **+17.02%** |
| b69157/b69158/b69159 宽 mux cycles | 13,769,354,332（1.558%） | 13,829,755,949（1.563%） | **+0.44%** |

b90656/b90657 的执行次数完全不变，绝对 cycles 只下降 **5.42%**，远未达到
s02 预注册的 20% 池级降幅；总 compute 与总 block cycles 也没有同步下降。因此，
虽然 e00108 的正式 Host 较 e00085 弱正 2.14%，现有证据不能把它归因于共享即时
谓词，`--sys-task-predicate-run` 作为 material residual-cost 机制被池级证据证伪，
不支持继续做同池微结构精修。

b93159/b93141 仍以 **8.569%** 占据首要主峰，绝对 cycles 仅下降 0.75%。e00108
不携带 `--commit-scratch-lazy-init`，所以本 recon 不评价 e00107 的独立弱正信号；
但后续若继续处理 commit，不能把本次近似持平误当成 scratch 后延的闭环证据。
六个 512-depth memory-read 块当前占 **3.101%**，且本 tip 尚未携带已在其他轨迹
确认的幂次索引专化；三块宽 mux 占 **1.563%**，也是未迁移的已确认池。两组正向
变化处于同一次插桩画像的波动背景中，只用于刷新权重，不能解释为语义工作量增加。

## 裁决与下一步建议

本 recon 成功刷新 r004/t4 的动态证据，功能门通过，预算保持 **34/48**。建议
r004/t4/s03 的 K=2 局部采样保持机制互异：

1. 唯一迁移席优先考虑已确认的 `--memory-read-pow2-index`，目标池当前占
   **3.101%**，并预注册保留 t4 谓词表型时的 Host 改善；不要在同一步同时迁移宽
   mux，以保持来源与增量可归因。
2. 本地席位优先面向 b93159/b93141 的 **8.569%** commit 主峰，但必须区别于已经
   回退或无加性的 bitpack、chunk=6000、lazy-init 组合等原样机制；若缺少新机制，
   可转向 b83835/b93085 的 **2.239%** 同构池并先证明静态覆盖。

本 action 只完成 recon，不启动后续 step。`record-recon` 登记后，状态机下一 action
应为 **r004/t4/s03 step（K=2）**。
