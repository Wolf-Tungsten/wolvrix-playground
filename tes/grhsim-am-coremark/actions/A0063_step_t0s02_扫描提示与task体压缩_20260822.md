# A0063：r003/t0/s02 扫描提示与 task 体压缩

- 日期：2026-08-22
- action：`step`（轨迹 `t0`，step 2/8，K=2）
- proposal：`proposals/r003-t0-s02.md`
- Φ 来源节点：`e00054`（`sys-task-body-outline`，score `-247560`）
- 评估协议：固定 3 rep、cores 12/13/14、50k cycles；两候选均经过完整 evaluator，包含 17/17 `ctest -R grhsim` 和逐 rep nemu difftest。

## 候选与结果

两候选都以 r003 的 10 个基础 emit 开关为基组，并显式携带
`--sys-task-body-outline`；c1 追加 `--scan-branch-hints`，c2 追加
`--sys-task-body-compact`。

| 候选 | commit | 机制假设 | eval / 状态 | compile_s | Host reps (ms) | 中位 / CV | 相对 e00054 |
|---|---|---|---|---:|---|---:|---:|
| c1 | `1563c3d837fcfe9db28fc36901531a70b59fd790` | 给 byte/Block 稀疏活动扫描的冷分支加 `__builtin_expect(..., 0)`，改善 fall-through 取指流 | e00057 / ok | 1064.0 | 229439 / 229429 / 229428 | 229429 / 0 | -7.32% |
| c2 | `5d820df245716cf7485135dffa376f7d6bbcb808` | 将无参数非 final `fwrite` outlined body 共享为 `sys_task_write_const(handle, formatOffset)`，保留参数型 body | e00058 / ok | 1072.4 | 252376 / 252374 / 252374 | 252374 / 0 | +1.94% |

两项的三 rep 均为 `instrCnt=73580`、`cycleCnt=49996`，退出码均为 0，功能门和
回归门全过。c2 比同 step 的 c1 慢 **10.00%**；两项均在 2400 s 编译预算内，
没有 `compile_timeout` 或干扰重测。

## 裁决

`finish-step` 按 score 机械选择 **c1/e00057**（score `-229429`），已快移入
`tes/r003/t0/main`，t0 tip 为 `1563c3d`，t0 best 更新为 e00057。c2 分支按
任务 retention 规则保留，未进入主线。

## 机制分析

- c1 的改动只影响扫描条件的分支预测提示，不改变 GRH 或运行语义。相对
  e00054 的名义下降为 7.32%，方向与 proposal 的前端流式假设一致；不过本轮
  两个读数仍是跨评估的机器态样本，不能把名义幅度全部当作机制幅度。它至少
  提供了当前轨迹上可继续验证的正向扫描轴。
- c2 将约 7,235 个 outlined `fwrite` 体中的无参数子集（约 5,937 个）合并到
  一个 noinline helper，确实保留了 fire、pending/once 收尾和输出顺序，且全门
  通过。运行时却比 outline 基座回退 1.94%，比 c1 回退 10.00%；共享调用、格式
  偏移物化和额外间接边界抵消了代码体积收益，因此该压缩形态在本输入上被证伪。
  生成 C++ 仍约 823 MB，compile_s 也没有改善。

 当前 `best_overall` 为 e00057（229.429 s），相对 r003 gsim 基线 45.864 s 仍约
  5.00x，目标尚未达到。下一状态机 action 预告为 `r003/t1/s02`；本笔记不启动
 下一 action。建议后续保持 t1 轨迹独立，优先用 recon/同窗证据检验扫描提示类方向，
 不携带已回退的 `sys-task-body-compact`。
