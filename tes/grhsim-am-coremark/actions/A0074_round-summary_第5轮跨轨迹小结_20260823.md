# A0074 round-summary - r003 第 5 轮跨轨迹小结（2026-08-23）

对应 `next` = `round-summary`（round 5）。本 action 无评估开销；数据来自
`state/ledger.jsonl`（r003 e00069-e00072）以及 A0072/A0073 step 笔记。

## 本轮各轨迹 winner 与假设

| 轨迹 | step | 候选 | Host 中位 | 状态/CV | 假设与裁决 |
|---|---|---|---:|---|---|
| t0 | s05 | e00069 c1 `scan-active-byte-prefix` | - | `ctest_fail` | 一次定位首个活动 byte、跳过 inactive prefix 后恢复 hinted 顺序扫描；16/17 grhsim 测试通过，但 emitter prefix 文本断言失败，未进入 emit/emu，功能门否决，无 winner |
| t0 | s05 | e00070 c2 `sys-task-fire-hints` | - | `ctest_fail` | 将非末端 fwrite fire guard 标为 cold；16/17 grhsim 测试通过，但 sys-task outline scaffolding 断言失败，未计时，功能门否决，无 winner |
| t1 | s05 | e00071 c1 nonzero-level bitmap | **339.910s** | `ok`, CV 0.02% | active-tile union 同时形成 <=64 层 nonzero bitmap，只重访实际非零层；较 e00067 名义 **-11.06%**，越过 2% 门，机械 winner，已入 `t1/main` |
| t1 | s05 | e00072 c2 priority lane resolve | 356.780s | `ok`, CV 0% | 从高优先级逆序解析并用 resolved mask 去除重复 lane 写；较 e00067 名义 **-6.64%**，越过 1.5% 门，但比 e00071 慢 **4.73%**，不入主线 |

e00069/e00070 的编译阶段分别为 316.3s/309.5s；两者均在 16/17 测试处停止，不能把机制记为性能负向。e00071/e00072 均通过 17/17 ctest、3 rep nemu difftest（73580/49996）和 2400s 编译门；compile_s 分别为 1876.0s/1870.1s。e00071/e00072 的起跑 loadavg 为 1.92/2.47，而父节点 e00067 为 12.30，因此相对 e00067 的绝对幅度仍受跨窗口状态混杂，只保留方向证据。

## 跨轨迹对比

- **收敛：连续数据流上的局部稀疏化继续有证据。** t1 从 zero-tile bypass 到 active-tile sparse，再到 union 时生成 nonzero-level bitmap，逐步减少二次遍历，同时保留连续 base 流；本轮 c1 比 priority 去重快 4.73%，说明“减少实际非零 level 重访”比“消除跨 level 重叠 lane 写”更有杠杆。下一轮应先取得 active tile 的非零 level 分布和重叠率，再提出增量变体，不原样重测 e00071/e00072。
- **发散：t0 的控制流精修仍未形成可评估证据。** prefix-skip 与 fire-hint 两候选都因测试契约断言失败而被功能门挡住；失败点是生成文本/脚手架断言，不是运行时语义或性能结果。后续 emitter 控制流候选须先用结构化不变量覆盖生成形态，修正版应使用新候选提交，不能原样重放失败分支。
- **共同测量约束仍有效。** 固定 3 rep、串行 evaluator 和 difftest 门保持不变；CV=0 只说明批内稳定，不能消除跨批快慢态。继续把跨窗口百分比用于方向筛选，不升级为机制净收益。

## 当前 run 与方向裁决

当前 t0/t1 各完成 5/8，候选评估计数为 22/32。`best_overall` 仍为 t0/e00057，Host
**229.429s**；AM 基线 e00051 为 **363.995s**，gsim e00052 为 **45.864s**，看板比约
**5.002x**，目标尚未达到。无需用户调整 C/L/K 或改变任务方向：保留 e00057 的
branch-hinted 顺序扫描与 t1 的局部 sparse/nonzero-level 证据，关闭 t0 两个未过回归门
候选的原样重开，并继续保持轨迹独立。

下一步建议：由 t0 先继续下一步预算，优先选择有明确结构化 emitter 契约且不破坏
fall-through 外形的机制；t1 仅在先完成动态计数量化后探索 nonzero-level sparse 邻域。
状态机登记后的下一 action 由 `tesctl` 决定；本 action 不调用 `begin-step`、evaluator
或其它下一步命令。
