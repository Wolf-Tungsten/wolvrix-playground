# A0111 step：r004/t2/s03 宽 mux 迁移与 commit scratch 清零后延组合（2026-08-24）

对应 action：`r004/t2/s03 step`，轨迹 `t2`，K=2，父 tip 为 e00104（Host
169.577s）。本 step 使用 A0110 recon 的动态证据：b93159/b93141 commit 主峰占
**9.261%** block cycles，b69157/b69158/b69159 宽 broadcast-to-mux 池占
**1.710%**。两个候选均保留 `--host-call-predicate-run` 与
`--memory-read-pow2-index`，并以完整 emit 表型通过硬审计。

## 候选与结果

| 候选 | commit | 来源 -> 动态病灶 -> 局部改动 | 正式结果 | 裁决 |
|---|---|---|---:|---|
| c1 / e00115 | `837b9dc59083` | e00104 + 迁移源 e00106 -> 宽 mux 池 1.710% -> 启用已确认的 ArrayBroadcast-to-ArrayMux 链融合，只物化尾值；预期 Host 至少改善 1% | **166.561s**（166.561/166.111/169.747ms，CV 1.18%，unimodal、非 noisy）；17/17 ctest，3 次 `73580/49996` difftest；compile_s **570.3s** | 较父改善 **1.78%**，低于 3% 确认带；`migration_source=e00106`，neutral winner，合入 `t2/main` |
| c2 / e00116 | `881f242dc6dc`（含修复提交） | e00104 -> b93159/b93141 9.261% 且事件门前 scratch 清零仍在 -> 将 chunk 共享 `arrChgblk`/`wrChgblk`/`detGrpblk` 清零延迟到事件门内；预期 Host 至少改善 1% | **170.194s**（170.194/168.733/172.436ms，CV 1.09%，unimodal、非 noisy）；17/17 ctest，3 次 `73580/49996` difftest；compile_s **582.2s** | 较父回退 **0.36%**，否决为非加性 |

## 实施与审计

- c1 迁移了 e00106 的 `--array-broadcast-mux-chain-fuse`，生产表型为冻结 12 项
  加 `--host-call-predicate-run --memory-read-pow2-index --array-broadcast-mux-chain-fuse`；
  迁移席位唯一且来源已登记。
- c2 的首次构建因 lazy-scratch 变量误落在非 chunk 函数作用域而 `build_fail`，未进入
  计时；修正后以同一 e00116 重跑，结果才登记。该修复提交为 `881f242`，不把首次
  失败当作性能证伪。
- c1 首次运行曾错误地使用冻结 12 项表型，`record-eval` 硬审计拒绝登记；随后以
  同一 e00115 和完整 15 项表型重测并覆盖，只有修正后的 166.561s 进入 ledger。

## 裁决与机制分析

c1 成为 raw-score winner，但只比 e00104 快 1.78%，未越 r004 的 3% 机制确认带，
因此 `finish-step` 判定 neutral。它说明宽 mux 融合在幂次索引基座上没有回退，
但不能证明 1.710% 动态池已经按预期降权；需要 post-change recon 才能把整体弱正与
池级因果分开。t2 轨迹 best 仍为 e00104（169.577s），全局 best 仍为 e00113
（166.014s）。

c2 的完整功能门通过但整体回退，说明把 scratch 生命周期后移与幂次索引/谓词 run
叠加没有净收益；b93159/b93141 的 9.261% 主峰不能仅靠这类清零位置调整消除。该
方向不再作为 t2 的局部细化优先项。

## 对 Φ 的建议

下一次先对全局 best e00106 做到期 recon（状态机下一 action），核验宽 mux 池在确认
组合上的实际降幅；t2 后续若再触碰 commit 主峰，应转向有新动态覆盖证据的批量/数据
布局机制，而不是重复 scratch 清零或 chunk 粒度扫描。
