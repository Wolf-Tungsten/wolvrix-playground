# A0088 recon：r004/t1/s00 e00085 动态热点画像（2026-08-23）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t1**；
被画像 tip：AM 基线 **e00085**（commit `1563c3d837fc`，完整 12 开关生产表型）。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00085 \
  --out build/tes/grhsim-am-coremark/recon/r004-t1-s00
```

recon 以 e00085 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本持有全局 `build/tes/LOCK`，运行期间
未并发任何构建或仿真负载。完整流程墙钟 **1012.9s**。profile run 得到
`instrCnt/cycleCnt=73580/49996`，与 AM 金标精确一致，difftest 门通过。产物位于
`build/tes/grhsim-am-coremark/recon/r004-t1-s00/`（`report.md`、`report.json`、
`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 Host 时间为 225.847s；它包含逐块 `rdtsc` 和计数开销，不是正式计时，
不得与 e00085 的 193.403s 基线相减或写入 score。

## 动态总账

| 项目 | 动态结果 | 解读 |
|---|---:|---|
| eval calls / rounds | 100,102 / 200,154 | **1.9995 rounds/eval**，调度轮数稳定 |
| compute | **164.898s / 73.1976%** | 596,466,270 次 compute-block 执行 |
| commit | **60.328s / 26.7794%** | 15,732,466 次 commit-block 执行 |
| other | 0.052s / 0.0229% | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | backward 是 forward 的约 1.184x |
| changed marks / clears | 200,264 / 200,264 | 成对出现，约 2.0006/eval |
| blocks / block cycles | 93,601 / 871,204,389,767 | 以下占比均以总块 cycles 为分母 |

commit block 仅占全部 block 执行的约 **2.57%**，却占阶段墙钟 **26.78%**；平均每次
commit-block 执行约为 compute-block 的 **13.87x**。commit 是高单次成本池，但
compute 的 73.20% 总量仍是更大的整体优化面。

热点集中度为：top-2 **8.495%**、top-5 **14.341%**、top-10 **18.546%**、top-20
**25.039%**、top-50 **39.714%**。top-50 中 36 个 commit 块占总 cycles
**27.716%**，14 个 compute 块占 **11.998%**；其外仍有 **60.286%** 长尾，单站点
微调的收益上限很低。

## 最高权重块

| rank | block | role / 静态形态 | execs | cycles | cycles% |
|---:|---:|---|---:|---:|---:|
| 1 | 93159 | commit，8,464 atoms/instrs | 100,103 | 49,998,512,926 | **5.739%** |
| 2 | 93141 | commit，12,189 atoms/instrs | 100,102 | 24,009,858,803 | **2.756%** |
| 3 | 90657 | compute，7,003 atoms / 7,068 instrs | 100,791 | 22,965,120,044 | **2.636%** |
| 4 | 90656 | compute，7,001 atoms / 7,757 instrs | 88,260 | 17,365,223,849 | **1.993%** |
| 5 | 83835 | compute，7,003 atoms / 8,927 instrs | 50,000 | 10,602,520,292 | **1.217%** |
| 6 | 93085 | compute，6,217 atoms / 8,125 instrs | 100,053 | 9,908,310,544 | **1.137%** |

- b93159/b93141 合计 **8.495%**，是最集中的 commit 池。生成形态中 b93159 含
  1,313 个宽站、870 个窄写站和 559 个 detect group；b93141 含 3,232 个宽站、
  814 个窄写站和 181 个 detect group。既有 run 已实测写点 branchless 与相邻行 RMW
  合并为负，当前权重不能支持原样复验，只有不同的数据布局或批量提交机制才值得重开。
- b90656/b90657 合计 **4.629%**，是最大的连贯 compute 池；生成代码仍密集排列
  outlined system-task 的 fire/条件求值与冷体调用。动态权重支持减少整个池的重复谓词
  与无效调用准备，不支持只给单个站点加 hint。
- b83835/b93085 合计 **2.354%**，包含宽值搬运、比较/归约和密集标量算术。两块每次
  执行成本高，但合计池上限有限；候选必须证明覆盖同构操作族，而不能只改一个源码点。

## 裁决与下一步建议

本 recon 成功刷新 r004/t1 的动态证据，功能门通过且不消耗 eval-id。r004/t1/s01 的
K=2 局部采样建议围绕两个机制互异的池展开：

1. 对 b90656/b90657 的 system-task fire/条件求值做池级公共谓词复用或分层短路，保持
   已 outline 的 task body 与 once/pending 语义不变；以该双峰 cycles 至少下降 20%
   且整体 Host 越过 3% 噪声门作为可证伪预期。
2. 对 b83835/b93085 的宽值搬运与标量算术选择一个可全池 engagement 的专化机制，先
   证明覆盖同构操作族，再要求对应块 cycles 明显下降；已知 **2.354%** 权重上限意味着
   不能承诺大跨度收益。

commit 池只有在提出区别于 branchless/行 RMW 的数据布局或批量提交机制，并能解释
`b93159/b93141` 的数据侧成本时才可重开。top-50 外 60.286% 的长尾要求优先选择
池级 pass，不以静态站点数替代动态权重。本 action 只完成 recon，不启动后续 step。
`record-recon` 登记后，状态机下一 action 应为 **r004/t1/s01 step（K=2）**。
