# A0057 step r002/t1/s08：concat 插入内联跨轨迹迁移同窗确认 -6.26%（2026-08-21）

- action: step（t1 第 8 步，K=2，run 最后一个机制步）
- eval 预算：候选口径 30/32 → 32/32（全部用完；含基线口径 34/34）
- 轨迹归属：A0055 裁定 s08 给 t1（池地图最陈旧 + compute 长尾为全 run 最大残余池），
  A0056 预算口径勘误后 t0/s08 已先行走完，本步为 r002 收官机制步

## 前置 recon（recon-t1s07：hinted 生产 emu perf，镜像 recon-t0s06/t0s07 方法）

A0055 纪律：t1 池地图（recon-t1s05）早于 fuse+hints 两个已落机制，须刷新到 tip 再
候选。对 e00029 生产 emu（tip 74b6d1e、7 旋钮、无插桩）全程 perf record（99Hz，
**45,319 样本**，Host 457.710s 慢态窗、difftest 金标过 73580/49996）。符号聚合
（覆盖 99.5%）：**eval_scan_* 44.35%** / block_chunk 外置块体 29.12% /
**helper 23.88%** / eval_commit_* 1.03% / eval() 自身 0.07% / libc 1.09%。

- **dispatch 骨架轴 t1 侧以直接证据关闭**：eval_scan_4_part_3 等 annotate 抽查，
  样本全部摊在内联块体 ALU（`and $0x3` 窄值掩码等）上，序言/位测试跳过链 ~0%
  ——与 recon-t0s07 同结论，hints 后跳过链已压出热点。
- **helper 首位易主：insert_words 11.61%**（≈53s 慢态窗），远超
  slice_words_detect 3.61% / array_mux_words 2.68% / index_words 2.14%。
  annotate 显示成本摊在动态 helper 全身（prologue 5 push、`mov $0x40,%ecx`
  19.17% 局部热点、动态移位/循环控制、epilogue pop 10.30%），无单点——对症
  机制 = 调用站常量特化内联，而非 helper 本体优化。
- block_chunk 细分：b90657 1.86% + b90656 1.45%（守卫链残余，两侧 outline 后
  仍存）、b93085 1.32%、commit 块 93xxx 族长尾各 0.6-0.8%。
- 静态盘点（e00029 emit）：insert_words 调用站 276,059，其中常量参数站
  276,055 的 **94.4%（260,733 站）为单字退化**（lsb%64+srcWidth≤64，含满字
  对齐 48,436 站）——t0 链 r001 起携带的 `--concat-insert-inline` 从未迁入
  t1 链，t1 是全 run 唯一未携带该机制的轨迹。
- 产物 `build/tes/grhsim-am-coremark/evals/recon-t1s07/`（perf.data、run.log）。

## 候选与假设（事先写下）

- **c1（e00033，`--concat-insert-inline` 迁移，d153d13，旋钮类 --allow-empty）**：
  单字退化 concat/replicate/window-chain splice 从 outlined insert_words 动态
  循环调用改为内联单语句 splice（满字对齐退化为直接 word store、死 zero_words
  前导消除）。r001 e00033 同族确认 -2.78%（旧图 959M 动态调用、池更小）；r002
  新图池份额 11.61% 为全符号首位。假设：Host 中位较同窗安慰剂降 **>=4%**；
  **<2% 证伪**。
- **c2（e00034，安慰剂锚点，b39dd6e）**：t1 tip 74b6d1e 原样 + 同 7 旋钮，
  连续第七轮锚点席位；无机制假设。

## 结果对比

| 候选 | eval | Host 中位 | reps | CV | compile_s | 裁决 |
|---|---|---|---|---|---|---|
| c1 机制 | e00033 | **338.367s** | 343718/338367/338366 | 0.91% | 799.9s | **winner，确认** |
| c2 安慰剂 | e00034 | 361.025s | 361025/361025/361034 | 0.0% | 847.8s | 锚点 |

两候选 17/17 ctest、3 rep difftest 全过（73580/49996）。**c1 同窗 -6.26%**，
越 4% 假设门；c1 最差 rep（343.718s）对锚点中位仍 -4.8%，裁决不依赖漂读。

## winner 裁决与机制分析

- **确认（e00033，已入 t1/main）**：-6.26% = 22.7s，占 insert_words 池（慢态窗
  ≈53s、按本窗锚点口径折算 ≈42s）的约半——与 r001 捕获形态一致（收掉的是
  outlined 调用边界 + 动态循环/移位装置，残余 = 真实 splice 工作内联后的
  ALU/访存本体）。
- **engagement 精确命中设计**：emit 静态站点 insert_words 276,059→**15,326**
  （-94.4%，残余 ≈ 15,322 跨 word 站 + 动态 lsb 站）；replace_window_words
  ~0。目标文件总量 115.3→105.6MB（**-8.4%**，调用+参数搭建比单语句 splice
  更大，外加 zero_words 前导消除）；emit 源 785.6→785.5MB 持平（文本级
  一进一出）。emu_build 364.2s 无膨胀（对照 r001 `--inline-wide-helpers`
  的单 TU 28min 事故——本旋钮只内联单字退化形态，编译侧安全）。
- **跨轨迹迁移三连中**：宽链融合（池比外推）× scan-branch-hints（捕获率外推）
  × concat-insert-inline（r001 同族 + 池份额放大）——t1 链自此与 t0 链在
  splice 内联轴对齐。「不省指令、改数据/取指流形态」判据第三次正向逆用成立
  （此处消除的是调用边界与动态装置，.o 反而 -8.4%）。
- **测量学**：本窗两候选各自批内 CV≈0（三 rep 同态），无批内双态混杂；锚点
  361.025s 较上窗锚点 342.230s +5.5% 慢向——跨窗漂移样本 +1（第八点），
  「跨窗读数不裁」纪律下本轮裁决仍只看同窗差。t1 真值口径 = 本窗锚点
  **361.025s**（e00034）、含 inline tip 本窗 **338.367s**（e00033）。
- t1 走完 8/8；t1 有效 emit_args = CLI 默认调度点 + 5 旋钮 +
  `--wide-mux-chain-fuse` + `--scan-branch-hints` + `--concat-insert-inline`
  （8 项）。

## 对 Φ 下一步的建议

- **残余 insert_words 15,326 跨 word 站 = r003 候选材料**：`--concat-insert-unroll`
  （t0 链已携带、t1 未携带；r001 e00039 弱正未确认 -1.89%）在 r002 新图上与
  inline 叠加后的边际未测——r003 restart 后可用单变量复测，静态池 15,322 站。
- **helper 榜刷新**：inline 后 slice_words_detect（3.61%）将升为 helper 首位，
  array_mux_words（2.68%）、index_words（2.14%）随后；commit 相纯数据侧
  （布局/预取）历届偏负的纪律不变。
- **run-summary 材料**：t1 真值口径曲线终点 = 本窗 338.367s（锚 361.025s）；
  t1 全步确认链 = s03 task-body-outline（-10.91%）、s04 调度点迁移（-8.44%）、
  s06 宽链融合（-1.17%）、s07 scan-branch-hints（-5.69%）、s08 concat 插入
  内联（-6.26%）；证伪 = s05 commit-write-branchless（+1.71%）。
- 协议升级提请维持（A0056：rep 级快/慢簇分组裁决）；基线重锚仍待用户裁决
  （vs 基线 ratio 在 run-summary 中以「不可裁」表述）。
