# A0103 step：r004/t4/s02 修正 fixture 完成双机制测量（2026-08-24）

对应当前 `next` = `step`，轨迹 `t4`，step 2/4，K=2。Phi 选择 e00085
（Host **193.403s**）作为 refinement 来源；最新 t4 recon 为
`build/tes/grhsim-am-coremark/recon/r004-t4-s00/report.md`。t4/s01 的两个机制均因
候选 fixture 断言错误被 ctest 门误报、性能未测，本 step 分别修正各自 fixture 后完成
正式测量；两项仍分别作用于 commit scratch 生命周期和 outlined host-call 谓词分支，
机制互异且没有使用跨轨迹迁移席位。

## 候选与结果

| 候选 | eval / commit | 来源 -> 动态病灶 -> 局部改动 -> 可证伪预期 | 正式结果 | 裁决 |
|---|---|---|---|---|
| c1 `--commit-scratch-lazy-init` | e00107 / `d38f1b8` | e00085 + t4/s01 c1 回归反馈 -> t4 recon 中 b93159/b93141 合计占总块 cycles **8.646%**，此前 fixture 错要本模型不存在的 detector-group 数组 -> 删除错误断言，保留事件门打开后才清零 chunked commit scratch 的实现 -> 热点池至少下降 15%、Host 至少改善 1%，否则证伪 | **189.851s**（190.700/187.554/189.851s，CV 0.86%，单簇、非 noisy），`compile_s=644.5s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00085 改善 **1.84%** | 达到 1% Host 预期但未越 3% 确认带；raw score 低于 c2，不合入主线 |
| c2 `--sys-task-predicate-run` | e00108 / `d3d4181` | e00085 + t4/s01 c2 回归反馈 -> t4 recon 中 b90656/b90657 合计占总块 cycles **4.923%**，此前 stock/outline/run 输出逐字节相同但 fixture 把 data 行数错写为 9 而非 15 -> 修正计数断言，保留相邻同即时谓词 outlined fwrite 合并为单分支的实现 -> 热点池至少下降 20%、Host 至少改善 1%，否则证伪 | **189.263s**（189.263/187.791/193.202s，CV 1.47%，单簇、非 noisy），`compile_s=640.8s`；17/17 ctest，3 rep difftest 均为 `73580/49996`；较 e00085 改善 **2.14%** | raw-score winner，outcome=`initial`，合入 t4/main；达到 1% Host 预期但未越 3% 确认带 |

两个候选的 `tes-candidate.json` 都相对 r004 冻结 12 开关表型声明各自一个新增开关；
正式 `result.json.emit_args` 与声明逐项一致。两次 evaluator 严格串行运行，只使用任务
评估器完成全量构建、17 项回归、金标 difftest 和绑核 3 rep，均远低于 2400s 编译门。

## 裁决与机制分析

- `finish-step` 按 raw score 选择 e00108，并将 commit `d3d4181` 快移到
  `tes/r004/t4/main`。t4 此前没有成功节点，因此 e00108 以 outcome=`initial` 建立
  首个 best；t4 Host 为 **189.263s**，冻结 gsim/e00086 口径为 **8.330x**。全局 best
  仍是 t3/e00106 **166.947s**，冻结 AM/gsim = **7.348x**。
- 两项都通过此前误报的回归门并达到各自预注册的 1% 整体 Host 下限，说明 s01 的
  `ctest_fail` 确为 fixture 问题而非生成语义失败；但 1.84%/2.14% 都在 r004 的 3%
  确认带内，只能记为弱正信号，不能宣称机制收益确认。
- e00108 只比 e00107 快 **588ms（0.31%）**，远低于 3% 分辨带。raw-score winner 是
  状态机的确定性选择，不构成 host predicate run 优于 commit scratch 后延的证据。
- 预注册的热点池降幅（commit 双峰 -15%、system-task 双峰 -20%）尚无 post-change
  recon 验证；整体 Host 达标不能替代池级机制证据。两项作用于不同阶段，当前也没有
  可加性结论。

## 后续建议

t4 下次 recon 到期时先对 e00108 做 post-change profiling，核对 b90656/b90657 是否按
预期降权并观察 b93159/b93141 是否仍为主峰；在池级证据前不继续谓词 run 微结构精修。
commit scratch 后延可保留为独立弱正机制，但不要仅凭与 e00108 的 0.31% 同窗差异关闭。
状态机给出的下一 action 是 **r004/t5/s02 step**；本 action 只完成 t4/s02，未启动下一
action。
