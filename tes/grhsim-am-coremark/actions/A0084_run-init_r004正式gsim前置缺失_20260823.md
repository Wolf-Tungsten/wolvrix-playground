# A0084 run-init：r004 正式 gsim 前置缺失（2026-08-23）

类型：run-init 前置失败。计划 run：**r004**（C=6, L=4, K=2，N=48）。
本次没有执行 `init-run`、基线评估或 `action-done`，r003 关闭态保持不变。

## 做了什么

1. 按 `tesctl.py status` / `next --json` 核对现场：r003 已收口，下一状态为
   `run-closed`；r004 已准备以 `r003/e00057`（commit
   `1563c3d837fcfe9db28fc36901531a70b59fd790`）为 y0，结构为 C=6/L=4/K=2。
2. 核对 run-init 全部本地前置：目标 checkout 干净且位于上述 commit；新迁移输入
   `build/xs/grhsim-am/wolvrix_xs_post_stats.json` 存在，SHA-256 为
   `c82ed4542f58ba60b1d7b38c57a877ad7cc81898a5df019d72d7e885588b70c7`；
   reference/gsim 与 testcase/xiangshan 当前 commit 分别为 `a6a5183f`、`8d96f2b3`。
3. 发现 config `paths.gsim_emu` 指定的正式 target
   `build/xs/gsim/gsim-compile/emu` 不存在。按任务 playbook 的硬规则立即停止，
   没有让 TES 自行构建 gsim，也没有先创建半初始化 r004。
4. 追溯唯一候选 `build/xs/gsim-flat/gsim-compile/emu`：SHA-256
   `eb21ecd8bf013bf5e64a38f6a540e96ef48d3ef0556cf64814fdd9c4b5909188`，构建于
   2026-07-31。`topo-partition-proj/docs/13-gsim-node打平coremark50k对比.md`
   明确记录它使用 `--flatten-nodes --supernode-max-size=16`，而正式基线使用未打平
   图与 `--supernode-max-size=15`；同文实测打平版比正式基线慢 3.6%。因此没有改
   config、复制二进制或建立软链来冒充正式 target。

## 量化结果与裁决

| 检查项 | 结果 | 裁决 |
|---|---|---|
| r004 y0 | r003/e00057，`1563c3d8`，12 开关表型 | 就绪 |
| 新 post-stats 输入 | SHA-256 `c82ed454...b70c7` | 就绪，初始化后应冻结此新指纹 |
| 正式 gsim emu | `build/xs/gsim/gsim-compile/emu` 缺失 | **硬阻塞** |
| 现有 gsim-flat | `--flatten-nodes` 实验版；历史对正式基线 +3.6% | 不等价，拒绝替代 |
| 本次新增 eval / ledger | 0 / 0 | 未污染实验台账 |

裁决：本次 run-init **未完成**。使用 gsim-flat 会把 target 定义从 gsim master
生产图偷换为打平图，直接改变目标水位；即使 difftest 通过，也不能证明性能基线等价。
保持 r003 关闭态比登记一个只有 AM 侧或错误 gsim 侧的 r004 更可恢复。

## 恢复条件与下一步

- 由用户在 TES 之外恢复未打平、无正式计时插桩的 gsim master emu 到精确路径
  `build/xs/gsim/gsim-compile/emu`。恢复后先核验来源/参数，再重新执行完整 run-init。
- 后续 run-init 应冻结上述新 post-stats SHA，预留 AM `e00085`、gsim `e00086`，
  并使用已改为逐次串行的 3-rep 起步协议重新测两侧，不能复用已 revert 的并行基线。
- 恢复前 `tesctl next` 仍应为 `run-closed`；不得执行 recon 或 step。
