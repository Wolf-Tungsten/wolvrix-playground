# A0115 step：r004/t4/s03 幂次索引迁移与窄 Replicate 乘法证伪（2026-08-24）

对应当前 `next` = `step`，轨迹 `t4`，step 3/4，K=2。Phi 选择 e00108
（Host **189.263s**）作为 refinement 来源；最新 recon 为
`build/tes/grhsim-am-coremark/recon/r004-t4-s02/report.md`。该画像中六个尚未专化的
512-depth memory-read 块占总 block cycles **3.101%**，b83835/b93085 混合 compute
池占 **2.239%**。两个席位分别处理已确认机制迁移与本地 Replicate 串行装配，机制互异。

## 候选与结果

| 候选 | eval / commit | 来源 -> 病灶 -> 改动 -> 可证伪预期 | 量化结果 | 裁决 |
|---|---|---|---|---|
| c1 `--sys-task-predicate-run --memory-read-pow2-index` | e00119 / `099335a` | e00108 -> t4 post-change recon 中六个 memory-read 块占 **3.101%**，当前 tip 缺少已确认的幂次索引专化 -> 以唯一迁移席移植 e00093，删除 3,072 个 `index_words` 调用和不可达越界分支 -> Host 较 e00108 至少改善 5% | **173.624s**（173.624/169.945/174.001s，CV 1.30%，单簇、非 noisy），`compile_s=572.0s`；17/17 ctest、三次 `73580/49996` difftest 全过；较父改善 **8.26%** | 达到预期并越过 3% 确认带；`migration_source=e00093`，outcome=`win`，合入 t4/main |
| c2 `--sys-task-predicate-run --replicate-multiply` | e00120 / `896ace8` | e00108 -> b83835/b93085 占 **2.239%**，b83835 有密集 1-bit x 8 串行 `concat_value` Replicate -> 对 <=64-bit Replicate 用掩码源值乘编译期扩散常数一次装配 -> 池降至少 15%、Host 改善至少 0.5% | **191.047s**（191.047/188.968/193.238s，CV 1.12%，单簇、非 noisy），`compile_s=643.1s`；17/17 ctest、三次 difftest 全过；较父回退 **0.94%** | 生产 b83835 的 192 个站点全部命中且旧 `replicate_` 链为 0，但整体回退；机制证伪，不合入 |

两项均使用冻结 12 项表型加父节点的 `--sys-task-predicate-run`，再分别增加自己的
候选开关；`tes-candidate.json` 与实际 `emit_args` 通过 record-eval 硬审计。

## 测量恢复说明

c1 首次调用 evaluator 时漏传两个 default-off 新开关，得到 **190.309s**，随后
`record-eval` 以“声明推导表型 != 实际表型”拒绝登记。该结果不是候选机制测量，未写
ledger、未占新 eval-id。按 playbook 恢复路径，以同一 e00119 显式传完整 14 项表型重跑；
上表与 ledger 只记录有效重跑的 **173.624s**。c2 在启动前即显式传完整表型，无重测。

## 机制分析与 winner

- e00119 将幂次索引叠加到 predicate-run tip 后仍取得 **8.26%** 干净改善，证明该一阶
  机制与 t4 的 host-call 谓词表型可加；t4 从 e00108 **189.263s** 推进到
  **173.624s**，相对冻结 AM/e00085 改善 **10.23%**，冻结 gsim 口径为 **7.642x**。
  本 action 没有 post-change recon，不能从整体 Host 反推六块池的精确降幅。
- e00120 是实现命中但成本假设失败：b83835 的 192 个窄站点确实由多次
  `concat_value` 变为单次乘法，功能也逐位一致，但 Host 回退 **0.94%**。这说明该池的
  一阶成本不在窄 Replicate 串行装配，或常数乘法成本抵消了前端缩短；
  `--replicate-multiply` 轴关闭，不做参数化重测。
- `finish-step` 按 score 与 3% 噪声带将 e00119 标为 `win` 并快移到
  `tes/r004/t4/main`。全局 best 仍是 t1/e00113 **166.014s**；e00119 比其慢
  **4.58%**，本 step 不刷新 best_overall。

## 对下一步的建议

状态机下一 action 为 **t5/e00110 recon**：先量化幂次索引迁移后六个 memory-read
块的动态降幅与替代热点，再设计 t5/s03；本 action 不启动该 recon。t4 若再次到期，
应先做 e00119 post-change recon，再决定是否组合已确认的宽 mux 链，避免继续细化已被
动态证据否定的 predicate-run 或窄 Replicate 微结构。
