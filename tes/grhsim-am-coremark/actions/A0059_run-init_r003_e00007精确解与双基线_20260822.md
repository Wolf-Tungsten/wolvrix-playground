# A0059 run-init：r003 从 r002/e00007 精确解启动并重测双基线（2026-08-22）

类型：run-init（覆盖初始化、输入指纹和全部基线测量）。run：**r003**
（C=2, L=8, K=2，搜索预算 N=32）。

## 做了什么

1. 按用户最终裁决，以 r002 台账最低点 `r002/e00007` 作为 y0，而非 A0058 建议的
   较晚 t0 tip。通过
   `tesctl.py init-run --run-id r003 --base-eval r002/e00007 --C 2 --L 8 --K 2`
   初始化；冻结的完整解为：
   - 代码 commit `ecb4c3f3c6b26cd0aed3491a1a9444959a4a73fb`；
   - e00007 的 10 个 emit 开关：`branchy-mux`、`resize-elision`、
     `init-zero-elision`、`source-part-activity-guard`、
     `source-word-activity-guard`、`wide-storage-first-touch`、
     `concat-insert-inline`、`inline-scalar-helpers`、
     `concat-insert-unroll`、`wide-detect-fast-path`。
2. 创建 `tes/r003/base`、`tes/r003/t0/main`、`tes/r003/t1/main`，三者均精确指向
   `ecb4c3f3`；隔离基线 worktree 为
   `build/tes/grhsim-am-coremark/src/base-r003`。只读 pin 为
   `reference/gsim@70f2ab1`、`testcase/xiangshan@8d96f2b`。
3. 将唯一输入图 `build/xs/grhsim-am/wolvrix_xs_post_stats.json` 的 SHA-256
   `cbd78c0b127dfb3bbbb005d06594242846a9d1cf35944de0720d7cf3031b3246`
   回填到 manifest 与 run.json。
4. eval-id 按任务历史续接：AM/gsim 双基线使用 e00051/e00052，下一候选从
   e00053 开始。两侧均按 r003 冻结协议固定 3 rep，在物理核 12/13/14 单批并行，
   不因 CV 扩增。

## 量化结果

| 基线 | eval | reps（ms，core 12/13/14） | 中位 | CV | 构建/功能门 |
|---|---|---|---|---|---|
| AM（y0 = r002/e00007 完整解） | e00051 | 363998 / 363995 / 363995 | **363.995s** | 0.00% | wolvrix 63.9s；emit 70.9s；emu_build 1598.2s；ctest 17/17；3 rep difftest 73580/49996 全过 |
| gsim（target） | e00052 | 45866 / 45864 / 45863 | **45.864s** | 0.00% | 3 rep difftest 73584/49998 全过 |

起跑差距：**363.995 / 45.864 = 7.936x**。两侧均 `status=ok`、`noisy=false`。
AM 基线的全部编译阶段在 run-init 一次性 5400s 预算内完成。

## 裁决与机制分析

- **解身份正确**：commit、emit 参数、来源 run/eval 三项均被冻结，且
  `prepared_solution`、ledger、e00007 result.json 交叉校验一致；不存在只继承代码
  而丢失参数表型的问题。
- **e00007 的旧绝对时间未复现**：e00051 比 r002/e00007 的 261.543s 慢 39.17%，
  但与 r002/e00012 同解慢态锚 363.444s 仅差 +0.15%。因此 r003 确实从用户指定的
  **完整解**起跑，而 261.543s 继续只是一条历史快态观测，不能当作本 run 的基线时间。
- gsim e00052 比 r002/e00002 的 46.792s 快 1.98%；两侧本批 CV 均为 0，当前
  7.936x 是 r003 的冻结起点差距，不复用 r002 的跨窗比值。
- K=2 两席均保留给 Φ 所选节点邻域内的实质候选，不设置固定安慰剂。TES 外失败只作
  先验，不自动进入本 run 的负节点集合。

## 对下一步的建议

- 唯一下一 action 是 `r003/t0/s01`；执行 `begin-step` 后，候选编号应为
  e00053/e00054。
- 两个候选都必须从 Φ 实际选中的节点出发，写清“来源节点 → TES 内反馈/病灶 →
  局部改动 → 可证伪预期”。本 action 不预选机制，也不越过状态机启动 step。
- 后续候选一律使用 run manifest 冻结的 10 开关基座、固定 3 rep 和 2400s 正式
  编译预算；e00051 的 5400s 只属于冷基线例外。
