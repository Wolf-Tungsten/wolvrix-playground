# A0080 round-summary - r003 第 7 轮跨轨迹小结（2026-08-23）

对应 `next` = `round-summary`（round 7）。本 action 无评估开销；数据来自
`state/ledger.jsonl` 的 r003 e00077-e00080、`corr-e00075-e00076-phenotype`，
以及 A0078/A0079 step 笔记。

## 本轮各轨迹候选、winner 与假设

| 轨迹 | step | 候选 | Host 中位 | 状态/CV | 假设与裁决 |
|---|---|---|---:|---|---|
| t0 | s07 | e00077 c1 `scan-active-byte-prefix` | **312.027s** | `ok`, CV 0, `compile_s=1063.3s` | 跳过 inactive byte 前缀后恢复 hinted 顺序扫描；完整 13 开关表型全门通过，但较 e00057 名义回退 36.00%，未过 -2% 门，无正向证据 |
| t0 | s07 | e00078 c2 `sys-task-fire-hints` | **299.715s** | `ok`, CV 0, `compile_s=1069.3s` | 将 outlined 非 final fwrite 触发边标为 unlikely；比 e00077 快 3.95% 并机械入 `t0/main`，但较 e00057 名义回退 30.63%，未过 -1.5% 门，无正向证据 |
| t1 | s07 | e00079 c1 round-local selector summary reuse | **359.866s** | `ok`, CV 0, `compile_s=1990.6s` | 四条 23-level 调用同 round 复用 selector union/nonzero-level 摘要；较有效父节点 e00071 回退 5.87%，摘要维护和跨 tile 预扫描成本更高，证伪 |
| t1 | s07 | e00080 c2 idempotent output store suppression | **345.215s** | `ok`, CV 0, `compile_s=1983.1s` | base/overlay 写前比较目标词，仅变化时 store；比 e00079 快 4.07% 并机械入 `t1/main`，但较 e00071 回退 1.56%，逐 lane compare/branch 未被省写抵消，证伪 |

四个候选均通过 17/17 ctest、3 rep difftest 和编译预算门；e00077/e00078 的计数为
73580/49996，e00079/e00080 同为 73580/49996。两条轨迹的候选表型均已在登记前
逐项审计：t0 是 e00057 的完整 12 开关父表型加候选开关，t1 是 e00071 的完整
13 开关表型。因而本轮不存在 A0075 式候选开关漏传。

## 跨轨迹对比

- **本轮没有新增正向性能证据。** t0 的两个候选均错过预注册收益门，t1 的两个
  候选均慢于有效父节点；e00078/e00080 的 raw-score winner 身份只决定轨迹主线，
  不应升级为机制收益。
- **t0 控制流微调收敛为“无动态权重不再细化”。** e00077/e00057=1.360x、
  e00078/e00057=1.306x，落在已知 1.3-1.4x per-process 快慢态带，故 36.00%/
  30.63% 跨窗回退不能全归因于 prefix/fire hint；但两项都没有任何达到收益门的
  正向观测。首活跃 bit 分布、实际跳过测试数、fwrite fire 次数和参数准备动态权重
  缺失前，静态站点数不能支持继续投入。
- **t1 wide-mux 静态代理精修明确发散。** e00079 证明四条调用共享 selector 仍不足以
  支付摘要容器与预扫描；e00080 证明 99.57% lane 最终取 base 不等于目标词已等于
  base。selector 共享度、base lane 比例、少量 helper ABI 均不能替代 target 词
  实际变化率和 helper 动态 Host 占比。
- **第 6 轮结论按追加勘误收窄。** `corr-e00075-e00076-phenotype` 证明这两个 eval
  缺少 `--wide-mux-chain-fuse`，实际未启用 active-tile sparse 候选机制；因此 A0076/
  A0077 对 mask cache 和 single-level direct 的性能证伪已失效。原 ledger 与笔记保持
  不改，以 correction 和 A0079/A0080 为准。

## 当前 run 与方向裁决

当前 t0/t1 各完成 7/8，总 eval 计数为 30/32。`best_overall` 仍为 t0/e00057，
Host **229.429s**；AM 基线 e00051 为 **363.995s**，gsim e00052 为 **45.864s**，
当前 best/gsim 约 **5.002x**，目标未达成。

无需用户在 run 中途调整已冻结的 C/L/K；预算只余 2 个 eval 席位。第 7 轮跨轨迹
结论只用于本小结和未来 restart，不回流 r003 的轨迹 proposal。剩余搜索若涉及上述
邻域，仍须从各自轨迹的 Phi 证据独立建立动态权重，不能依据本跨轨迹比较直接选候选。
登记后 `tesctl next` 给出的下一 action 是 `r003/t0/s08 step`（K=2）；本 action 不调用
`begin-step`、evaluator 或任何下一步执行命令。
