# A0090 recon：r004/t2/s00 e00085 动态热点画像（2026-08-23）

类型：recon（非计时 profiling，不占搜索评估预算）。run：**r004**；轨迹：**t2**；
被画像 tip：AM 基线 **e00085**（commit `1563c3d837fc`，完整 12 开关生产表型）。

## 执行与功能门

执行命令：

```bash
python3 tes/grhsim-am-coremark/recon.py --eval-id e00085 \
  --out build/tes/grhsim-am-coremark/recon/r004-t2-s00
```

recon 以 e00085 的同 commit `grhsim-am-lower-json` 和生产 `emit_args` 追加
`--runtime-profile`，重 emit 并构建独立 emu；脚本全程持有 `build/tes/LOCK`，
运行期间未并发任何构建或仿真负载。完整流程墙钟 **994.5s**。profile run
得到 `instrCnt/cycleCnt=73580/49996`，与 AM 金标精确一致，在线 difftest 门通过。
产物位于 `build/tes/grhsim-am-coremark/recon/r004-t2-s00/`（`report.md`、
`report.json`、`block_execs.txt`、`profile.log` 和插桩 emit）。

profile run 的 Host 时间为 230.234s；它包含逐块 `rdtsc` 和计数开销，不是正式
计时，不得与 e00085 的 193.403s 基线相减或写入 score。本 action 没有
候选、新 eval-id 或性能裁决。

## 动态总账

| 项目 | 动态结果 | 解读 |
|---|---:|---|
| eval calls / rounds | 100,102 / 200,154 | **1.9995 rounds/eval**，调度轮数稳定 |
| compute | **167.341s / 72.8696%** | 596,466,270 次 compute-block 执行 |
| commit | **62.250s / 27.1071%** | 15,732,466 次 commit-block 执行 |
| other | 0.053s / 0.0233% | 非一阶成本 |
| activations | forward 1,693,784,931 / backward 2,005,232,887 | backward 是 forward 的约 1.184x |
| changed marks / clears | 200,264 / 200,264 | 成对出现，约 2.0006/eval |
| blocks / block cycles | 93,601 / 886,956,056,267 | 以下占比均以总块 cycles 为分母 |

commit block 仅占全部 block 执行的约 **2.57%**，却占阶段墙钟 **27.11%**；
平均每次 commit-block 执行约为 compute-block 的 **14.1x**。commit 仍是高单次
成本池，但 compute 的 72.87% 总量不能被 commit 局部优化替代。

热点集中度为：top-2 **8.599%**、top-5 **14.342%**、top-10 **18.500%**、
top-20 **25.187%**、top-50 **39.947%**。top-50 中 35 个 commit 块占总 cycles
**27.747%**，15 个 compute 块占 **12.200%**；其外仍有 **60.053%** 长尾，
单站点微调的收益上限很低。

## 最高权重块

| rank | block | role / 静态形态 | execs | cycles | cycles% |
|---:|---:|---|---:|---:|---:|
| 1 | 93159 | commit，8,464 atoms/instrs | 100,103 | 50,989,841,530 | **5.749%** |
| 2 | 93141 | commit，12,189 atoms/instrs | 100,102 | 25,280,095,877 | **2.850%** |
| 3 | 90657 | compute，7,003 atoms / 7,068 instrs | 100,791 | 23,890,472,825 | **2.694%** |
| 4 | 90656 | compute，7,001 atoms / 7,757 instrs | 88,260 | 16,671,003,905 | **1.880%** |
| 5 | 83835 | compute，7,003 atoms / 8,927 instrs | 50,000 | 10,365,452,389 | **1.169%** |
| 6 | 93085 | compute，6,217 atoms / 8,125 instrs | 100,053 | 9,558,160,941 | **1.078%** |

- b93159/b93141 合计 **8.599%**，是最集中的 commit 主峰。两块的分块形态
  在事件前缀 chunk 调用前分别无条件零初始化 2,742（`1313+870+559`）和
  4,227（`3232+814+181`）个 `arrChg/wrChg/detGrp` 布尔槽；前缀 chunk 本身
  只更新边沿事件，真正写回 chunk 受单事件或双事件条件保护。这是新的
  「事件后延迟 scratch 初始化 + chunk 参数活性裁剪」证据，不是已证伪的
  branchless、行 RMW 合并或 commit-input 门控原样重试。
- b90656/b90657 合计 **4.574%**，仍是最大的连贯 compute 池。两个生成 TU
  中静态含 6,349 个已 outline 的 `sys_task_body_*` 调用，大量相邻站点重复
  `fire && changedResults_[0]` 形状的完整谓词。动态权重支持池级精确同谓词
  run 合并，不支持再次 outline 冷 task body 或只改单站点 hint。
- b83835/b93085 合计 **2.247%**，以宽值搬运、标量比较/归约和紧密算术为主。
  它们的已知权重上限低于前两池；任何后续候选必须证明覆盖同构操作族，
  不能只用静态站点数代替动态收益预测。

## 裁决与下一步建议

本 recon 成功刷新 r004/t2 的动态证据，功能门通过且不消耗 eval-id。
r004/t2/s01 的 K=2 局部采样建议保持机制互异：

1. 优先在 chunked commit 发射中做参数活性分析，让只做事件检测的前缀 chunk
   不再接收 watch/detector 数组，并把数组声明与清零移到事件成立后。要求
   b93159/b93141 两块都生产命中；可证伪预期是双峰 cycles 至少下降 15%、
   整体 Host 下降至少 1%，低于 0.5% 或事件实际每次都打开则证伪。
2. 在 b90656/b90657 中只合并连续且谓词完全相同的 outlined task run，将共享
   fire/event 谓词与 handle 物化提到单一分支内，不改 pending/once 语义，也不做
   整块门控。要求生产 engagement 覆盖双峰；可证伪预期是该池 cycles 下降
   至少 20%、Host 越过 1%，低于 0.5% 则关闭该形态。

两个建议均只使用 t2/e00085 的本次 recon 证据；下一 proposal 仍必须保持
r004 run 内轨迹独立，不引用 t0/t1 当前轨迹的候选结果。本 action 只完成
recon，不启动后续 step。`record-recon` 登记后，状态机下一 action 应为
**r004/t2/s01 step（K=2）**。
