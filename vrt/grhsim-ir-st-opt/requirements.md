优化grhsim-ir路线的单线程仿真 xiangshan coremark 50k 性能，参照物gsim仿真时间~40s， 同时要求编译时间 <30min 。
鼓励的优化手段：扩展grhsim-ir语义支持，提供更多高性能的op及emit方法，改善分区调度算法，实现行为等价但性能更优的子图替换等 。
禁止的优化手段：修改已冻结的grh ir及grh上的pass，修改xiangshan以及测试相关源码，采用多线程加速，生成针对特定模块名称匹配的优化算法 。
每个方向在完成任务时要报告优化后 xiangshan coremark 50k 的实测单线程性能，注意，测试性能需要wolvrix-playground复杂的构建环境（build目录），请充分复用，不要尝试自己搭建。

补充需求（虚拟第 1 周 PI 派发，2026-09-09；以上原始需求完整保留）：

- R=3，每位 RA 固定 6 次成功工程调用，开工均为 0/6；PI/RA 调用不计费。所有 RA 用满配额、完成逐步审查和周报后才进行 PI 总结，至多选一个通过验收的候选，不拼接方向。
- 每方向交付完整 xiangshan coremark 50k 目标的实测单线程性能、编译耗时、正确性和可比证据；明确输入/参数、计时边界、证明/证伪判据。gsim 约 40s 是需求参照，不能当作已复测数据。
- 必须复用入口 build/ 和 wolvrix/build/，不得另搭构建环境。构建、测试、安装只能经项目 Makefile；没有所需目标时先补目标再调用。临时日志放入口 ptmp/，产物遵守既有目录约定。
- 每方向首次工程调用须实际尝试完整目标，直到完成或暴露最早实际阻塞。辅助任务只解除具体阻塞；连续两步没有目标推进、阻塞缩小或新增独立验证证据，须由 RA 改变方法。工程师自行定位、尝试修复普通错误。
- 三方向由 PM 安排从入口 d4e802932c3e5f777963263c7db798ab398041eb、wolvrix b9931ed852532d161fea06d0ee0b5bbe07c43466 同一技术基点分支隔离，串行使用现有环境；历史 worktree 和 ptmp/98b594db-week-1-ra-1/ 保持原状，不算本项目成果，不互相复制方向实现或生成物。
- 档案固定在 /home/gaoruihao/wksp/wolvrix-playground/vrt/grhsim-ir-st-opt/。本次 PI 只在入口现有 grh/grhsim-ir 分支显式暂存并提交项目档案，提交使用 docs: 前缀并标明项目和周次；不修改技术代码、gitlink，不切换分支，不提交或手工改写 .runner/。此要求优先于 PI 模板的提交前缀。
- 失败重试先核对已有文件、Git 改动和提交，保留有效工作；当前没有未知技术调用，不恢复历史实验。报告采用“结论；依据/动作/缺口”，保留必要证据。
- 所有角色开工先通过指定 Makefile 读取 inbox 历史，再 consume；实际时间至多每 3 分钟及结束/提交前重复检查，本人读取结果并吸收其他 Agent 新领取的历史记录。长命令可轮询，单次等待不超过 60 秒；不手动截断 inbox，不改写历史/回执。有效要求及未完成项持续交接，完整路径与命令见 week_1/pi_plan.md。

运行中补充（2026-09-09 22:02:15 +08:00 inbox 消费记录）：

> 编译的时候用16线程并行 -j16

编译统一使用 16 路并行（-j16），须核实实际构建子进程采用该并行度；仿真仍为单线程，禁止通过多线程加速仿真。
