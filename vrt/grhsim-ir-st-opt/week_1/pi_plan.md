# grhsim-ir-st-opt：虚拟第 1 周 PI 方向规划

结论；设 3 个独立方向：RA 1 语义/op/emit、RA 2 分区调度、RA 3 等价子图。共同目标为优化 grhsim-ir 单线程 xiangshan coremark 50k 实测性能，同时完整编译耗时严格小于 30min；gsim 约 40s 仅为需求参照。本文固定目标、边界与验收，不规定实现方法、工程步骤或技术验证命令，由 RA 拆解、工程师自主实施。

依据；已读 requirements.md、project.json、../workflow/vrt-workflow.md、../workflow/prompts/pi_plan.tmpl.md（后两者相对项目目录），以及当前执行回执。执行编号为 `grhsim-ir-st-opt-week-1-pi-plan`。原始需求保留在 requirements.md 与 project.json.initial_requirements，补充要求追加至 requirements.md。本周无历史周报、工程结果或未知技术调用。

## 1. 开工版本与责任

2026-09-09 22:02 +08:00 只读复核结果如下；各 Git 状态/版本检查退出码为 0。

| 对象 | 分支 / HEAD | 现场与责任 |
| --- | --- | --- |
| 入口 `/home/gaoruihao/wksp/wolvrix-playground` | `grh/grhsim-ir` / `d4e802932c3e5f777963263c7db798ab398041eb` | 仅当前项目档案未跟踪，索引无既有改动；本次仅提交档案 |
| 子模块 `/home/gaoruihao/wksp/wolvrix-playground/wolvrix` | `grh/grhsim-ir` / `b9931ed852532d161fea06d0ee0b5bbe07c43466` | 工作区干净，入口 gitlink 与该 HEAD 一致；本次不修改 |
| 既有环境 | 入口 `build/`、`wolvrix/build/` 均存在 | 后续方向必须复用，不新搭环境；PM 安排串行占用与来源核查 |
| 历史现场 | `ptmp/98b594db-week-1-ra-1/` 及 Git 列出的其他 worktree 均存在 | 只确认存在，保持原状；不得借用为本项目实现、生成物或实测成果 |

动作；本次不切换分支、不修改技术代码或 gitlink，不手工改写或提交 `.runner/`。按用户要求使用 `docs:` 前缀、项目名和周次提交，优先于模板前缀。显式暂存 project.json、requirements.md、week_1/pi_plan.md；档案提交 SHA 随本次交接提供，由 PM 核查。

后续技术分支隔离由 PM 安排，从上表同一技术基点开始；档案提交不改变技术基点。RA/工程师开工及完工记录实际目录、分支、前后 SHA、未提交状态、输入和环境指纹。不同方向不复制实现或生成物；只可复用已核实来源相同的公共输入与既有基础设施。若发现污染、基点偏移或不可比，交 PM 安排核查与重测，不能沿用混杂结果。

## 2. 三个方向

| 方向 | 目标 | 可检验假设 | 价值 | 边界 | 预期证据 |
| --- | --- | --- | --- | --- | --- |
| D1 / RA 1：grhsim-ir 语义、op 与 emit | 降低完整目标中操作执行与表达开销，使实测单线程时间改善并满足编译上限 | grhsim-ir 对行为的表达及其 emit 存在可消除的执行开销；保持语义后，收益能体现在完整 50k 运行，且编译代价可控 | 提供可用于其他设计的高性能语义/op/emit 能力 | 仅在 grhsim-ir 及其后端范围；不得改变冻结 GRH IR/pass；以操作表达与执行为主，不将另两个方向成果并入候选 | 变更语义及等价边界说明、实际命中与开销变化证据；完整目标的前后时间、编译时间、正确性与同口径 gsim 对比，逐项满足 A1–A8 |
| D2 / RA 2：单线程分区调度 | 降低完整目标中分区组织和调度成本，使实测单线程时间改善并满足编译上限 | 当前分区与调度组织引入可避免的执行、检查或局部性开销；在保持时序/依赖语义下优化组织可获得整体收益 | 改善大规模模型在单 CPU 上的运行效率，检验性能与编译规模的共同约束 | 调整限于 grhsim-ir 后续分区与单线程调度；不引入并行仿真，不按模块名称特判；不以改变操作语义或复制 D1/D3 实现代替本方向 | 依赖、状态更新与调度等价证据；调度成本/分区特征变化与整体时间联系；完整目标的前后时间、编译时间、正确性与同口径 gsim 对比，逐项满足 A1–A8 |
| D3 / RA 3：行为等价子图优化 | 在 grhsim-ir 层减少完整目标中的冗余计算或数据移动，使实测单线程时间改善并满足编译上限 | 存在由结构和语义识别的等价子图替换，能减少实际工作量，收益不会被新调度/编译成本抵消 | 获得可泛化、可解释、可证明适用条件的图级优化 | 替换在 grhsim-ir 内完成；禁止修改冻结 GRH IR 及其 pass、按特定模块名匹配、依赖该测试数据写死结果；不拼入其他方向候选 | 替换前后行为、适用前提与反例边界、目标中实际覆盖及计算/搬运变化；完整目标的前后时间、编译时间、正确性与同口径 gsim 对比，逐项满足 A1–A8 |

各方向允许为自身目标作必要的局部支撑调整，但须说明归属和因果联系，不以范围扩展变相拼接方向。具体实现、定位方法、工程步骤、验证命令由 RA 决定；局部收益不能代替整体收益，局部测试不能代替最终验收。

共同禁止；修改已冻结的 GRH IR 及其 pass、修改 xiangshan 和测试相关源码、采用多线程加速仿真、按特定模块名称匹配优化。允许扩展 grhsim-ir 语义、高性能 op/emit、改善分区调度及行为等价子图替换。新建/调整 helper 须遵守入口 AGENTS.md 的 legacy 性能参考和数据搬运约束。需要验证时使用既有测试及不改测试源码的实验材料；不能以增加测试为名突破源码禁区。

## 3. 固定输入、计时与比较口径

结论；三个方向的基点 grhsim-ir、最终候选、gsim 使用相同的冻结目标输入和运行条件。RA 应将实际解析后的参数、路径与内容哈希写入方向报告，不依赖 Makefile 默认值或历史目录名称推断可比性。PM 核查三方记录一致；本次 PI 未运行性能或编译实验，所有实测数值仍待交付。

| 项目 | 固定口径与必要记录 |
| --- | --- |
| 完整目标 | xiangshan `SimTop`、单核设计配置，既有 `testcase/xiangshan/ready-to-run/coremark-2-iteration.bin`，既有 `riscv64-nemu-interpreter-so` 正确性参考；50k 固定为现有周期上限参数的 50,000 周期（`XS_SIM_MAX_CYCLE=50000`），不是 50k 指令或缩小电路。RA 核实计数器/复位边界并记录实际终点；若入口解释不符，报告差异，不能自行缩短目标 |
| 输入指纹 | 两层仓库 SHA、相关依赖版本、RTL/filelist/宏/初始化配置、冻结 GRH 输入及其来源、二进制与参考库哈希、grhsim-ir 配置、最终模型与可执行文件哈希。保留所有实际生效参数，包括随机种子、复位和日志配置；未知值必须查清 |
| 运行条件 | 仿真仅一个执行线程，绑定同一可用 CPU；记录 CPU 型号/编号、线程与亲和性证据、频率策略、内存、OS、编译器及优化选项。三个方向串行测量，计时期间不并行构建或跑其他方向；不能只凭 CPU 绑定声称单线程 |
| 诊断条件 | 正确性检查开启，波形、性能插桩、提交追踪、RAM 追踪及周期进度输出在排名运行中关闭；必要诊断运行单列，不混入性能样本。普通日志级别与缓冲方式统一；禁止为了计时关闭正确性检查 |
| 仿真计时 T | 单个已构建仿真进程启动前至其退出后的单调时钟 wall time，包含加载、初始化、50k 执行、正确性检查和退出；不含编译、派发等待。内部循环时间、CPU time 可辅报，不能替换 T。保存起止、退出码、实际周期与原始输出，识别超时/提前退出 |
| 编译计时 C | 在既有环境内，从启动本候选所需项目构建/更新开始，包含 grhsim-ir 转换/优化、emit、目标生成 C++ 全量编译及最终链接，至可运行 emu 产生；各阶段与总 wall time 均记录。共同冻结的已有 RTL/GRH、第三方依赖和工具链可复用，但必须列明起点、哈希及排除项，三个方向和基点一致；若需重新生成某项，其耗时计入。禁止用增量/no-op、旧候选目标文件或候选对象缓存命中冒充全量编译耗时；环境复用不等于复用候选产物 |
| 编译资源 | inbox 新增要求固定为 16 路并行 `-j16`；核实嵌套构建的实际并行参数，不能只记录顶层标志。严格要求 C < 1800s，等于 1800s 也不通过。共享目录由 PM 串行协调，不清理或覆盖历史 worktree；不重新搭建环境 |
| 样本与参照 | 每方向为同条件基点 grhsim-ir、最终候选、gsim 各提供至少 3 次有效完整运行，列全部样本、最小值/中位数/最大值，以中位数作为 T。无效样本保留并说明原因，不能挑最快值。至少提供一次符合上述边界的候选完整编译计时。三个方向均交付自己候选的实际证据，公共参照若引用其他记录，须是本项目同条件可核查原始证据并核验适用性，不能复制方向生成物 |
| 40s 的含义 | `约40s` 是用户给出的历史参照，当前未复测，不作为分母或基线实测值。报告实际 gsim 中位数 T_g、候选与 T_g 的比值，并另述距约 40s 的差距；若本机实测不同，如实保留。没有 gsim 实测则比较证据未闭合。40s 不是本周新增硬性通过阈值；必须证明相对同基点 grhsim-ir 的实际改善 |

证明口径；设基点中位数 T_b，候选中位数 T_i。报告加速比 T_b/T_i、改善率 1−T_i/T_b、相对 gsim 比值 T_i/T_g。为避免把波动当收益，性能验收要求候选中位数更低且候选样本最大值小于基点样本最小值；样本范围重叠时记“收益未证实”，由 RA 决定如何独立复核，不能选择性丢样。完整同条件测量若候选样本最小值仍不小于基点最大值，记“该候选性能假设在此目标下被证伪”。只对所测候选和输入下结论，不推论整个方向不可能有效。

## 4. 固定编号最终验收表

每个 RA 周报必须逐行给出“通过 / 不通过 / 未完成”、证据路径和缺口；不得重编号、删项或降低输入规模。A1–A8 全通过才具备候选资格。

| 编号 | 原始需求 / 完整验收目标与输入 | 通过证据 | 不通过、证伪或未完成判据 |
| --- | --- | --- | --- |
| A1 | 完整 xiangshan coremark 50k；采用第 3 节全部输入与参数 | 最终候选实际运行完整 50,000 周期，参数、计数边界、终点、退出码及原始日志齐全 | 电路缩小、周期不足或输入替换不通过；未启动/未跑完是未完成，不是性能证伪；约定周期上限结束不要求两轮 CoreMark 自然运行结束 |
| A2 | 保持完整目标正确性与候选行为等价 | 完整运行启用既有 difftest/参考检查且无失配/断言错误；解释终止语义，给出与同输入基点的可观测行为一致证据及方向变更适用条件、独立边界/反例审查 | 任一真实行为失配证伪该候选的正确性；仅退出码 0、局部回归或静态等价声称不足；缺完整检查记未完成 |
| A3 | 优化 grhsim-ir 的实测单线程性能 | 单线程与 CPU 证据、基点/候选全部 T 样本、汇总及改善满足第 3 节证明口径 | 多线程或有意减轻目标不通过；有效证据无收益按既定判据报告；实验缺失不作证伪 |
| A4 | 编译时间 <30min，编译并行度 16 | 相同既有环境和冻结起点，C 的全阶段原始记录、实际 `-j16`、候选产物来源完整，C <1800s | C ≥1800s 或完整构建超出上限不通过；增量/no-op 冒充全量、缺阶段或并行度未核实则未完成 |
| A5 | 以 gsim 约40s 为参照且比较可复核 | 本项目同条件 gsim 实测、T_i/T_g、T_b/T_i、全部输入/环境/计时与样本证据齐全；明确40s的非实测身份 | 用约40s冒充复测、条件不一致、方向污染不通过；参照未测或原始证据缺失则未完成 |
| A6 | 禁区、版本隔离与环境复用 | 变更清单及两层前后 SHA/差异审查证明未改冻结 GRH/pass、xiangshan/测试源码，未用多线程或模块名匹配；复用入口 build/、wolvrix/build/，未借历史或其他方向成果 | 任一越界不通过；缺变更/来源审查为未完成。不得用性能收益抵消违规 |
| A7 | 完整目标优先、Makefile 执行、工程自主修复 | 首次工程调用完整目标实际尝试记录；每步目标联系、退出码、日志、真实阻塞与修复尝试；辅助工作接回目标的证据；所有构建/测试/安装经 Makefile，临时日志在入口 ptmp/ | 用预计耗时替代首次执行、外围任务未关联阻塞、连续两步无推进且未调整、普通错误无调查、绕过 Makefile 均不通过；最终未闭合仍报告目标未完成 |
| A8 | 每 RA 用满 6 次成功工程调用并完成审查周报 | 6 个成功工程任务及回执对应、6 次逐步 RA 审查，方向周报逐项核验 A1–A8，最终候选精确提交/输入/产物/日志可追溯 | 用量不足、审查/周报缺失不得进入 PI 总结；失败重试不重复扣费，CLI 成功不能代替技术通过 |

未实现、未启动、超时未完成、缺输入或比较证据统一标注缺口，不写“方向无价值”。已发生的正确性反例、有效的性能无收益或编译超限，可证伪该候选对应主张；不能据此免除本方向剩余工程调用。

## 5. 共同排名与周末选择

结论；全部 RA 达到 6/6、完成逐步审查和周报后，PI 才做最终验收与排名。只在 A1–A8 全通过、同口径可比的最终候选中至多选一个；不拼接方向，全部不合格就报告目标未完成和缺口。

排名固定如下：

1. 以完整目标候选仿真中位数 T_i 从小到大排列，同时公布 T_b/T_i、T_i/T_g 和 C；不能按局部性能、代码量或静态 op 数排名。
2. 两候选仿真样本区间重叠时视为性能并列；重叠关系传递闭包构成并列组，避免三方向出现循环排名。不凭微小中位数差宣称优胜；并列组优先选择 C 更低者。若编译测量边界/条件不一致则先补可比证据，不以猜测破同分。
3. 编译耗时相同或差异无法可靠区分时，优先选择等价边界更清楚、独立正确性证据更充分、适用限制更少的候选，PI 必须引用具体证据。仍无法区分则保留并列、选择零个候选，不按 RA 编号或报告完成顺序硬选。

准确性、单线程、编译上限与合规都是门槛，不作可互相抵消的加权分数。方向可以证明达到或超过本机 gsim，也可以仅证明相对基点改善；两者在报告中明确区分。选定后由 PM 派发精确提交的集成与完整目标复验，集成成功不能由规划或排名代替。

## 6. 配额、推进与交接

| RA | 本周成功工程调用配额 | 当前已用 / 剩余 | 初始状态 |
| --- | --- | --- | --- |
| RA 1 | 6 | 0 / 6 | 待 PM 核查规划后拆步 |
| RA 2 | 6 | 0 / 6 | 待 PM 核查规划后拆步 |
| RA 3 | 6 | 0 / 6 | 待 PM 核查规划后拆步 |

PM 按稳定 task_id、回执和实际结果核算，总计 18 次成功工程调用；PI/RA 不计费。同一任务 CLI 失败重试不重复扣费，新工程工作不能伪装重试。各任务串行派发；阶段性 done/no_value 不得提前结束，剩余调用用于原方向独立复核、反例、边界或有依据的修订候选，不能仅重复写报告。

每方向首次工程调用必须实际尝试完整目标流程直到成功或最早实际阻塞，不能先做纯静态分析或以预计长耗时免执行。辅助任务须指明具体阻塞、结果如何影响下一步及如何接回完整目标。连续两步既未推进目标、未缩小阻塞、也无新增独立验证时，RA 须复核现场、改变方法并给出故障位置、修复假设与验证命令；这些命令由 RA 制定。工程师在一次调用内持续定位、最小修复和验证普通错误，只有实际尝试仍无法继续才提交具体阻塞。

档案固定保存于 `/home/gaoruihao/wksp/wolvrix-playground/vrt/grhsim-ir-st-opt/`：任务/结果/审查在 `week_1/ra_<i>/steps/step_<k>_{task,result,review}.md`，周报在 `week_1/ra_<i>/report.md`，周末 PI 报告在 `week_1/pi_final_report.md`。临时日志在入口 `ptmp/`，构建产物使用既有约定；报告必须链接原始日志，不仅保留提炼结论。各报告简明，复杂内容采用“结论；依据/动作/缺口”，保留参数、命令、退出码、路径、版本和未完成项。

运行中指令持续有效；共享 inbox 为 `/home/gaoruihao/wksp/wolvrix-playground/vrt/grhsim-ir-st-opt/.runner/inbox.txt`，历史为同目录 `inbox-history.jsonl`。所有角色及重试开工先执行并本人读取以下两项，实际时间至多每 3 分钟及提交/结束前重复，吸收其他 Agent 新领取的历史记录：

```text
make --no-print-directory -C /home/gaoruihao/wksp/wolvrix-playground vrt_inbox VRT_INBOX_ACTION=history
make --no-print-directory -C /home/gaoruihao/wksp/wolvrix-playground vrt_inbox VRT_INBOX_ACTION=consume
```

使用已设置的 VRT_INBOX/VRT_INBOX_ACTOR；长命令采用可轮询会话，单次等待不超过 60 秒，间隙检查时钟和 inbox。非空消息立即简短确认影响，不作 shell 执行，不自行扩大授权；不得手动截断 inbox、改写历史/回执。消费不等于完成，有效要求及缺口必须交接；若派生 Agent，传递本节、相同路径和检查命令。

当前交接；已消费“编译的时候用16线程并行 -j16”（2026-09-09 22:02:15 +08:00），已纳入 requirements.md、第 3 节与 A4，尚待工程实际落实。当前仅完成规划，无性能、编译、正确性实验结果；RA 均仍为 0/6。下一动作由 PM 核查本文件三个方向、A1–A8、排名规则及档案提交 SHA，再派 RA 拆步和安排技术隔离；本次不启动 RA 或工程调用。
