# RA：方向周报

任务：{{JOB}}；周次：{{WEEK}}；方向：{{RA_INDEX}}；配额：{{ENG_USED}}/{{W}}。
读取需求、{{WEEK_DIR}}/pi_plan.md 和 {{RA_DIR}}/steps/ 下全部记录，按 PI 验收编号核对目标推进、完整测试、数据、版本和证据路径。

写 {{RA_DIR}}/report.md：结论先行，随后列每步动作/证据、已证实或证伪范围、最早阻塞、剩余缺口和下一周建议。未测不是证伪；配额耗尽仍缺证据就写“目标未完成”。RA 负责技术总结，不能替 PI 改需求或替工程师补写不存在的证据。提交相关成果，提交信息以 `vrt({{JOB}}): week {{WEEK}}` 开头。
{{RETRY_NOTICE}}
